# SEO daily run — 2026-09-21 (WATCHDOG, cloud)

Still inside the expected 402 Hobby-plan usage-pause window (lifts on its
own ~2026-09-24, 3 days from today). No re-escalation trigger fired. One
new evidence-backed finding this run, filed to BACKLOG.md, not fixed today
(watchdog mode does not ship code).

## Live status measured this run (2026-09-21 21:37 UTC)

| Check | Result | Evidence |
|---|---|---|
| `GET /` | **402** | `x-vercel-error: DEPLOYMENT_DISABLED`, `x-vercel-id: iad1::rxmlp-1790026644556-89012e629a8e` |
| `GET /archive-sitemap.xml` | **402** | same error class |
| google-site-verification TXT | present | `google-site-verification=jXmPQSZbqumoPn0AKH4swGuYMoUDBFkJ4oq_4PrD2rY`, TTL 14400 — confirmed by both `dns.google` and `cloudflare-dns.com` |
| Open `[auto-alert]` issues | 6 open (#11, #14, #15, #16, #17, #18) | same set as every check since 09-13; none new today |
| `data/gsc-daily.json` | fresh this cycle, `generatedAt` 2026-09-21T14:30 UTC (a separate scheduled `gsc.yml` run, not this loop) | 7-day trend window 09-12→09-18: impressions 2 vs prior 3 (-33.3%), clicks 0 vs 0; `explained: true` in the file — this is the outage already logged in `data/incidents.json`, not a fresh decline |

None of the three re-escalation triggers fired: status code and
`x-vercel-error` are unchanged from every check since 2026-09-14, the TXT
record still resolves, and nothing measured this run contradicts the
usage-pause reading.

## Finding: `data/cwv-history.json`'s two newest PSI entries are measuring the 402 page, not the site

While reading the newest data for the outage caveat, the 2026-09-14 and
2026-09-21 PSI entries in `data/cwv-history.json` both show `/` and
`/top-100` scoring **100, LCP ~800ms** — during a run where `/` is
independently confirmed 402 with a 78-byte plain-text body. A tiny error
page loads near-instantly and scores perfectly under Lighthouse, so this
is PSI faithfully measuring the *error page*, not evidence the site is
fast. `scripts/cwv-check.mjs` already has a liveness guard for the third
(story) URL — `firstLiveStoryUrl()`, added 2026-08-21 after the identical
bug produced a fabricated story-page LCP — but that guard was never
extended to the two hardcoded surfaces (`/` and `/top-100`), so they still
record an error page's vitals as if they were the homepage's. No fix
applied this run (would need a live 200 to verify against, and this is a
watchdog day). Filed to `seo/BACKLOG.md` so it queues for when the full
loop resumes. Any reading of `data/cwv-history.json` between 2026-09-14 and
whenever this ships should treat the `psi` entries for `/` and `/top-100`
as not meaningful.

## Owner checklist

**Nothing.** The usage pause clears itself ~2026-09-24 (three days from
today). The only open item remains a decision, not a task: scope of the
slim relaunch, still deferred by the owner.
