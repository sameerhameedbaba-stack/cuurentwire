# SEO daily run — 2026-09-03

Run 2026-09-02 21:25–23:15 UTC (2026-09-03 02:55–04:45 IST).

## Outcome first

**Day 3: the site is fine, and it still cannot ship code.** Production has been
serving the same build since **2026-08-31 12:43 UTC** — pinned exactly this run,
not merely bounded. Five commits are on `main`, green in CI, and not serving.

Two things changed today, and both are the run's own work:

1. **The blindness is fixed.** `deploy-watch.yml` now asks the one question no
   check in this repo asked, every 3 hours, and it already fired: **issue #8,
   `[auto-alert] production is not shipping pushed code`**, opened by the
   workflow itself in CI. Verified live — that is not a local test result.
2. **The diagnosis moved, and it changes what the owner should look at.** Every
   failing deployment is rejected in **under one second**. No Vercel build
   installs dependencies and runs `next build` in under a second, so **no build
   is being attempted**. The build log is very likely empty or irrelevant. The
   answer is a project/account-level condition — read the deployment's error
   banner, and Settings → Git and the account's billing state.

**Still the one thing needing the owner, and it is unchanged in size (~2 min).**
Push notification sent with the narrowed instruction.

Search Console data is unchanged from yesterday's pull (generated
2026-09-01 22:35 UTC): the decline stands, `explained: true`, named below and
not escalated.

## 1. Alerting and production health — checked first

| Surface | Result |
|---|---|
| `https://currentwire.us/` | **200**, 0.71 s |
| `/archive-sitemap.xml` | **200**, 14,124 permanent story URLs |
| `/news-sitemap.xml` | **200**, 738 entries, newest 0.2 h old |
| `/rss` | **200**, valid with items |
| `/sitemap.xml` | **200**, 356 URLs |

`node scripts/seo-health.mjs` against production: **ALL CHECKS PASSED**
(exit 0) — 28 checks including the 4-hour news-sitemap stall guard, 738 sitemap
URLs answering 200 (6 benign mid-check rename races, 5 merge races cleared on
re-check), 11 category feeds, llms.txt, the IndexNow key file, story JSON-LD,
the `www` 308 and a real 404.

Open `[auto-alert]` issues at the start of the run: **two**.

- **#7 daily SEO health check failing** (opened 2026-09-02 11:46 UTC). The same
  check passes from here now, so this was **transient**. Its Actions log needs
  admin rights to download, so the specific failing check could not be read —
  recorded rather than guessed at. The next scheduled green run closes it
  automatically.
- **#6 published story URLs are failing the survival check** — the standing one
  triaged 2026-09-02; its two genuine tombstones are in `593e369`, which
  **cannot take effect until deploys work**.

A third, **#8**, was opened during the run by this run's own new detector. That
is the intended behaviour, not a regression.

## 2. Performance trend — checked before any other work

`data/gsc-daily.json` is unchanged since yesterday's dispatch (generated
2026-09-01 22:35:43 UTC, `lagDays: 3`), so these are the same complete-day
numbers, re-read rather than re-fetched:

| Window | Clicks | Impressions |
|---|---|---|
| Aug 16–22 (prior 7) | 25 | 1,611 |
| Aug 23–29 (last 7 complete) | 4 | 234 |

**−84% clicks, −85.5% impressions**, `explained: true` — the window overlaps
`data/incidents.json` (Neon egress outage Aug 20–21, the Vercel 402 on Aug 24,
the archive write-burst stall Aug 26–27, Google de-indexing 11 of 12 evergreen
hub/category surfaces). Per the standing rule this is **named, not escalated**.

Aug 30 (0 clicks / 9 impr), Aug 31 (1 / 4) and Sep 1 (0 / 0) remain inside
`lagDays: 3` and are partial — they must not be read as a further collapse.

Against the **Sep 8 checkpoint** ("has the Google slide stopped?") the answer on
complete data is still *not yet*, and the qualifier from yesterday now applies
for a third day: the checkpoint will measure a site that has received **no code
change since Aug 31**.

`gsc.yml` was re-dispatched at the end of this run (within the once-a-day cap)
so the Sep 8 read has the freshest complete days available.

## 3. The shipping outage — three findings, all new

### 3a. Which build is live, pinned exactly

Yesterday's evidence (the footer newsletter form absent from the served HTML)
proves production is *not the newest* commit. It does not say which commit it
**is**. `f8805af` touched `public/llms.txt` — a byte-comparable static asset:

```
live https://currentwire.us/llms.txt   sha1 3c4df5b3e093
repo @ f8805af  public/llms.txt        sha1 3c4df5b3e093   <- identical
repo @ f8805af^ public/llms.txt        sha1 aa5735651280   <- differs
```

**Production runs `f8805af`, built 2026-08-31 12:43 UTC.** The first commit that
failed to ship is `72e30e7` at 21:39 UTC the same day. Independently confirmed:
the newsletter form's label and its `buttondown.com` action both appear **zero**
times in the served homepage, and both appear in a local production build of
the current tree.

### 3b. A correction: the deploy signal was never noise

Yesterday's report concluded the GitHub deployments API was unusable — *"both
Vercel projects report `failure` for all 30 recent deployments, including ones
that demonstrably shipped, so failure has carried no information for days"* —
and filed the follow-up as an `/api/version` endpoint on that basis.

Re-measured this run by resolving **each deployment's status individually**:

| commit | UTC | environment | state |
|---|---|---|---|
| `f8805af` | 08-31 12:45 | Production – currentwire | **success** ← what production serves |
| `4b64c6e` | 08-28 22:35 | Production – currentwire | **success** |
| `72e30e7` | 08-31 21:39 | Production – cuurentwire | failure ← first that did not ship |
| every deployment after `72e30e7` | | both projects | failure |

There is a clean boundary in the data and it sits exactly where production's
served bundle stops. The most likely cause of yesterday's reading is that it
listed deployment *records* without fetching each one's *status*, so every row
looked alike.

**This matters beyond the correction: the `/api/version` design could not have
worked.** It requires deploying a change in order to detect that changes cannot
be deployed. During the outage it was designed for, it would have sat merged and
dark alongside every other fix. Written up as
`seo/MEMORY/2026-09-03-a-signal-dismissed-is-a-signal-unread.md`.

### 3c. No build is being attempted — where the owner should actually look

Timing every deployment from creation to its final status:

```
failures  status written +0s or +1s after the deployment record   (24 of 24 sampled)
f8805af   success ~99 s after the push
```

A Vercel build clones, installs dependencies and runs `next build`; it cannot
finish in under a second. **The deployment is being rejected before any build
starts.** That reframes the owner's two minutes: the build log is likely empty
or beside the point, and the useful surfaces are the deployment's **error
banner**, **Settings → Git** (two projects, `currentwire` and `cuurentwire`, are
still wired to this repo) and the **account's billing state** — the site was
taken fully dark by a Vercel billing condition once already, on 2026-08-24.

This is inference from timing, not a read of Vercel's own error text, and is
labelled as such. `npx vercel inspect <id> --logs` would settle it but needs a
login this machine does not have; the Actions log route needs repo admin.

## 4. Shipped: `deploy-watch.yml` — verified live in CI (`68d33b2`)

The check whose absence made a 3-day shipping outage invisible. It needs
**nothing from the site** — no `/api/version`, no build stamp — which is the
whole point: it has to work *while* deploys are broken.

Two rules, because the first has a blind spot:

1. a failed deployment standing with no success after it → not shipping;
2. a HEAD commit older than 2 h with **no deployment record at all** → also not
   shipping. Without rule 2, disconnecting the integration would freeze the
   newest record at `success` forever and the check would go permanently green.

**Exit 1 (not shipping) and exit 2 (could not read the API) are kept distinct,
and only exit 1 opens the alert issue.** A rate limit is not evidence of an
outage and must never page the owner as one — a lesson this run learned the
direct way, by exhausting the anonymous 60 req/h budget mid-investigation.

**Live evidence, on GitHub's infrastructure, not locally:**

```
workflow run  Deploy watch  -> failed (the correct verdict)
issue #8      [auto-alert] production is not shipping pushed code   OPENED
              body: "…did NOT reach production… merged fixes are not live…
                     Vercel -> CurrentWire -> Deployments -> newest failed one"
```

Verified before push against a stub replaying this run's **real recorded API
shapes**: outage → exit 1 naming the stranded commits with `f8805af` as last
success; healthy → exit 0; dead endpoint → exit 2; preview deployments ignored.
15 unit cases in `tests/unit/deploy-watch-lib.test.ts`.

One implementation note worth keeping: `process.exit()` with undici sockets open
aborts Node on Windows with a libuv assertion and reports **127**, which would
have made every real verdict unreadable from this machine. The script sets
`process.exitCode` instead.

## 5. Shipped, not live: the newsletter form was blocked by our own CSP (`d3f8128`)

Found by reading production's response headers, not by any test. The sitewide
policy said `form-action 'self'`, while `components/layout/NewsletterSignup.tsx`
posts to `https://buttondown.com/api/emails/embed-subscribe/currentwire`.

**A browser blocks a cross-origin form submission that `form-action` does not
name, and logs nothing server-side.** The footer signup would have subscribed
**nobody**, silently, from the moment it went live — on a feature whose entire
purpose is collecting addresses, and against a Buttondown account currently in
new-sender review. It has never run in a browser, because it has never shipped.

The policy moves to `config/csp.ts` so it can be tested at all. The guard does
not assert a string: it **reads the form's own `action` attribute out of the
component** and requires the policy to name that origin, so changing providers
cannot silently re-open the hole. Proved by restoring `form-action 'self'` and
watching it fail.

**Verified on a real production build locally** (`npm run build && npm run
start`): header served as `form-action 'self' https://buttondown.com`, and the
form renders with its Buttondown action. **Not verifiable on production** — it
has neither the form nor the fix, for the same reason as everything else.

## 6. Crawl samples — all clean

Three newest `/story/` pages from the news sitemap plus two section pages:

| Page | Canonical | Robots | JSON-LD |
|---|---|---|---|
| `…rosetta-technologies-appoints-…-ca5b2cc32d1da` | self ✓ | index, follow, max-image-preview:large | NewsArticle (pub 09-02 21:30Z, isBasedOn 1, Business) + BreadcrumbList ✓ |
| `…asi-debuts-video-…-c32605fc1e897` | self ✓ | index, follow ✓ | NewsArticle (pub 09-02 21:30Z, isBasedOn 1, Technology) ✓ |
| `…nba-hits-l-a-clippers-…-c4954bda0f125` | self ✓ | index, follow ✓ | NewsArticle (pub 09-02 21:30Z, isBasedOn 1, Sports) ✓ |
| `/business` | self ✓ | index, follow ✓ | BreadcrumbList + ItemList parse ✓, one h1 |
| `/world` | self ✓ | index, follow ✓ | BreadcrumbList + ItemList parse ✓, one h1 |

No accidental noindex, no duplicated story, dates sane, sections match the
headlines. Nothing to file.

## 7. Gates

Both commits passed all five before push:

```
npx vitest run      976 passed, 3 skipped (68 files)
npx tsc --noEmit    exit 0
npx eslint .        0 errors (2 pre-existing warnings, unrelated)
npm run build       success
npx playwright test 118 passed, 1 skipped, 1 failed
```

**The one Playwright failure is the documented pre-existing flake**, not a
regression: `[mobile] drawer opens and navigates`. Re-run in isolation it passed
**3 of 3**. Yesterday's run established the same suite fails ~1 test per full
run, a different one each time, and reproduced it on a stashed clean tree (2 of
3 runs). Neither change this run touches page rendering — one is a CI workflow,
the other a response header that `form-action` alone governs. Still filed as a
known flake rather than waved away.

## 8. What regressed

**Nothing on production, and nothing in the record.** The correction this run
makes is to yesterday's *reasoning* (section 3b), not to a claim about the live
site — yesterday's central finding, that production is serving a stale build,
is confirmed and now pinned to a specific commit.

## 8b. Left alone: another session is editing this tree concurrently

The tree was **clean at the start of this run**. Partway through, two files
appeared modified without this run touching them — `config/site.ts`, then
`app/layout.tsx` — removing `social.x`, `social.facebook`, `social.linkedin`
and the `twitter.site` handle.

They are **not this run's work and were not committed by it.** They belong to a
concurrent session finishing the social-profile question that today's weekly
off-page run (`b795e3e`) opened. The new comment in `app/layout.tsx` carries
evidence the committed ledger does not have — it names `@currentwire` as *"a
real but unrelated X account (a dormant Tamil-language profile registered in
2017)"*, where `seo/offpage/LEDGER.md` still records that handle as *"X blocks
automated fetches, unverifiable from here"*. So this is fresh verification
landing, not an edit that was argued out of, and the change looks right:
production currently serves `twitter:site = @currentwire`, attributing every
page to a stranger.

**This run left both files untouched** — not committed (they are not its work
and not its evidence), not discarded (an uncommitted edit is still work). The
session that made them should commit them.

One disclosure this forces: **this run's gates ran against a moving tree.** The
`vitest`/`tsc`/`eslint`/`build`/`playwright` pass happened while
`config/site.ts` was already modified. The two pushed commits do not contain
that edit. Nothing is left unverified by the difference — the changes are
independent (a CI workflow and a response header), `site.social` is read
nowhere in the codebase, and `tsc` plus `build` pass either way — but the
mismatch is stated rather than glossed over.

## 9. What's next

1. **Owner: the Vercel dashboard** — but look at the deployment's error banner,
   Settings → Git and billing state first, not the build log (section 3c).
2. Once deploys work: confirm `deploy-watch` goes green and auto-closes #8;
   flip `6c63626`, `593e369` and `d3f8128` from MERGED to SHIPPED with live
   evidence; confirm `url-survival.yml` goes green on the two tombstones.
3. Backlog **00b** (three `data/` files compiled into the bundle while `data/`
   is excluded from deploys) stays deliberately unshipped — changing deploy
   config while deploys are failing adds a second variable.
4. Resume the strategy queue: LCP remains item 1.

## Owner blockers

**One, unchanged in size, sharper in aim.** Vercel has rejected every deployment
since 2026-08-31 21:39 UTC. Open the Vercel dashboard → CurrentWire →
Deployments → newest failed one, and read **the error banner** rather than the
build log — the sub-second failures say no build is being attempted. While
there: Settings → Git (two projects appear wired to this repo; disconnect the
stale one) and the account's billing state. ~2 minutes.

Everything else in this report is done, filed, or automated.
