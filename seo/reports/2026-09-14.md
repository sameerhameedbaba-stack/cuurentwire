# SEO daily run — 2026-09-14 (WATCHDOG, cloud) — 2nd attempt

An earlier run today logged a full egress block (see prior version of this
file in git history, commit `cd19280`). This run's outbound HTTPS worked
(one transient `ws_closed_mid_exchange` on the first `/` attempt, succeeded
on retry) and reached currentwire.us directly.

## Site status — CHANGED since the last confirmed check

| Check | Result this run | Previously (2026-09-12 → 09-13, confirmed) |
|---|---|---|
| `GET /` | **HTTP 402**, `x-vercel-error: DEPLOYMENT_DISABLED`, body "Payment required" | HTTP 503, `DEPLOYMENT_PAUSED` |
| `GET /archive-sitemap.xml` | **HTTP 402**, `DEPLOYMENT_DISABLED`, "Payment required" | 503, `DEPLOYMENT_PAUSED` |

This is not the same failure mode as the outage tracked since 2026-09-12.
`DEPLOYMENT_PAUSED` (Pause Projects, budget protection) has become
`DEPLOYMENT_DISABLED` with an explicit "Payment required" body. Vercel
serves this error when deployments are administratively disabled, typically
for an unpaid invoice — consistent with `data/incidents.json`'s note that
the final Pro-plan invoice (~$65.91) and Neon marketplace usage ($14.74)
were expected to post once after the 2026-09-15 downgrades. Whether that
invoice is now overdue is not knowable from here (no billing-dashboard
access in cloud mode) — flagged to the owner below, not fixed or guessed at.

## DNS TXT (google-site-verification)

Checked via DNS-over-HTTPS (`dns.google` and `cloudflare-dns.com`, both
agree): the only TXT record at the apex is still
`v=spf1 include:_spf.mail.hostinger.com ~all`. **No
`google-site-verification` record present** — same as every prior check
since this was first found missing on 2026-09-14. Search Console ownership
remains at risk.

## GSC numbers (outage caveat — not re-fetched this run)

Newest committed data (`data/gsc-report.json`, generated 2026-09-14T14:22Z,
28-day window ending 2026-09-14): 433 URLs / 1,296 impressions / 40 clicks
web-wide. These numbers predate today's 402 finding and already reflect the
503-outage collapse from the prior week — do not read them as a reaction to
today's status change, and do not carry them forward as current once the
site's state changes again.

## Alerts

Open `[auto-alert]` issues: 6 (#11, #14, #15, #16, #17, #18), all opened
2026-09-07 through 2026-09-13, all already known/attributed to the ongoing
outage chain in `data/incidents.json`. No new alert issue opened today.

## Owner checklist

1. **New finding, not yet actionable by this routine:** production now
   returns `402 DEPLOYMENT_DISABLED` ("Payment required") instead of the
   `503 DEPLOYMENT_PAUSED` seen since 2026-09-12. This usually means Vercel
   has disabled deployments over an unpaid invoice. Needs the owner to open
   the Vercel billing dashboard, check for an outstanding invoice, and
   decide whether to pay it (still consistent with the $0-*going forward*
   rule — this would be settling a charge already accrued under the old Pro
   plan, not enabling anything new) — this routine cannot view or pay
   invoices and will not guess.
2. Restore the `google-site-verification` TXT record at Hostinger DNS
   (unchanged, still missing).
3. Whether/when to unpause or otherwise restore the deployment is still an
   owner/strategy call, not something this routine does on its own —
   doubly true now that the failure mode has changed to a billing block.
