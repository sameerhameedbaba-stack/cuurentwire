# SEO weekly deep run — 2026-09-20 (PAUSED-SITE MODE, cloud)

Run 2026-09-20 22:0x UTC. **Not scored — site paused.** Production has
answered a non-200 on every surface for 8 straight days (503 for 2 days,
402 for the 6 since). Per `seo/routines/weekly.md` paused-site mode, this is
the short honest scoreboard the pause allows: pause status, days down, the
latest GSC numbers with the outage caveat, the TXT record, and the one
decision pending. No audit score, no PSI.

---

# THE SCOREBOARD

| Item | Reading | Evidence, this run |
|---|---|---|
| Site status | **402 `DEPLOYMENT_DISABLED`** on `/` and `/archive-sitemap.xml` | `curl -D-` 2026-09-20 22:04 UTC: `HTTP/2 402`, `x-vercel-error: DEPLOYMENT_DISABLED` |
| Days down | **8** (since 2026-09-12 ~11:49 UTC, 503→402 switch on 09-14) | `data/incidents.json` outage entries |
| Days left in expected pause window | **~4** (self-clears at the Hobby billing-cycle reset, ~2026-09-24) | Owner-verified dashboard reading, 2026-09-15 (see MEMORY note) |
| google-site-verification TXT | **present** | `dns.google` and `cloudflare-dns.com` DoH, both agree: `google-site-verification=jXmPQSZbqumoPn0AKH4swGuYMoUDBFkJ4oq_4PrD2rY`, TTL 14400, alongside the unchanged Hostinger SPF record |
| GSC clicks / impressions (7d window) | **0 / 3** vs prior 4 / 29 (−100% / −89.7%) | `data/gsc-daily.json`, window 2026-09-04→09-10, `generatedAt` **2026-09-14** — **6 days stale**, not re-pulled (production is 402 so `gsc.yml`'s own read targets would hit the outage, not Google) |
| GSC avg position / indexed pages | not re-measured this run | last sampled reading is the 09-14 weekly's; no fresher fetch exists to report |
| Total visitors (all channels) | **GA4: needs owner** (dashboard-only, no cloud access) | Bluesky public profile re-checked live: `followersCount: 0`, `postsCount: 58` — unchanged from the 09-14 weekly, i.e. nothing new posted since the outage began |
| Newsletter subscribers | **needs owner** (Buttondown dashboard, no public API) | — |
| Open `[auto-alert]` issues | 6 open (#11, #14, #15, #16, #17, #18), same set as every check since 09-13 | GitHub issues API, this run |

**If the numbers are bad: they are unchanged-bad, not new-bad.** Nothing
degraded further this week — the site simply stayed down for a 7th and 8th
day. The GSC collapse (0 clicks, 3 impressions) is the same outage already
explained in `data/incidents.json`, not a fresh decline.

---

## What moved

**Nothing.** Nine cloud firings since 09-15 (2 daily x9, this weekly) have
all measured the identical state: 402 `DEPLOYMENT_DISABLED`, TXT present,
no new auto-alert issue, Bluesky flat at 58 posts / 0 followers. This is
the expected shape of a Hobby usage-pause window — it is supposed to look
like nothing is happening until the cycle resets.

## What did not move

- The 402, for the 6th straight day.
- Google Search Console data — 6 days stale because there is nothing new
  for Google to crawl or for this loop to fetch from a 402 origin.
- Bluesky: 0 followers since account creation (2026-08-31).
- The 6 open auto-alert issues: same set, no new one since #18 (2026-09-13).

## The single most important thing pending

**The scope of the slim relaunch, still the owner's call.** Nothing on this
board can move until either (a) the usage-pause clears on its own at the
cycle reset (~2026-09-24, in ~4 days) and the *same* aggregator resumes and
re-exhausts the Hobby caps within days — or (b) the owner decides what the
slim relaunch actually serves before that date, per the strategy agreed
2026-09-15 (10 original articles/day, not mass aggregation). No code change
from this loop can make that decision or ship it; it needs the owner's
scope call, not more monitoring.

---

## Why this run is unscored

`seo/routines/weekly.md` paused-site mode: skip the audit score, skip PSI.
Every facet that would be scored — crawl infrastructure, story template,
list templates, E-E-A-T, GEO, performance — depends on production serving
something other than a 402 page. It doesn't. Scoring it again would repeat
the 09-14 weekly's "7/100 as served" reading without any new evidence, which
rule 1 (no claim without evidence from this run) rules out. The last
code-state reading (81, from 09-07, pre-outage) still holds and should be
re-measured only once production serves 200.

## Deliberately not attempted this run

- **PSI / Core Web Vitals** — no live site to measure.
- **Competitor content-gap snapshot** — our side can't be fetched; a
  one-sided comparison isn't evidence.
- **`llms.txt` refresh / coverage-map re-verification** — no pages changed,
  nothing to verify live.
- **Striking-distance queries (`data/gsc-queries.json`)** — still the
  09-11 pull; the site has earned effectively 0 impressions since, so
  re-reading it produces no new prioritisation signal.
- **`gsc.yml` / `cwv.yml` triggers** — both read production endpoints;
  dispatching them now would test the keep-previous-file path against a 402
  origin, not measure anything real.
- **Backlog re-prioritisation** — the ordered queue at the top of
  `seo/BACKLOG.md` is unchanged from the 09-14/09-18 readings; no new
  evidence this run to reorder it against.

No code changed. No gates were run because nothing was pushed.

---

# Owner checklist

**Still just the one pending decision, not a new task:**

1. **Scope the slim relaunch** before ~2026-09-24, so it's ready the moment
   the Hobby usage pause clears on its own. (Standing since 2026-09-15 —
   see `seo/MEMORY/2026-09-15-cloud-migration-and-current-state.md`.)

**Nothing else needs you this week.** The 402 is not a bill to chase, the
TXT record is intact, and no new alert fired. Dashboard-only reads this
loop still can't reach on its own: GA4 total visitors, Buttondown
subscriber count, Bing Webmaster AI-citation metrics, GSC Manual
actions/Security issues/Crawl stats/Sitemaps report — same as every run
since the cloud migration, listed here only for completeness, not as new
asks.
