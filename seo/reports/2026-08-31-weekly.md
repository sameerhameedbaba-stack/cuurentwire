# Weekly deep SEO run — 2026-08-31

**Score: 86 → 83/100 (−3).** Revised down twice while this report was being
written, both times because a number I had published turned out to be wrong.
The site's plumbing genuinely improved this week — the archive write burst, the
redirect-loop mechanism, the story-freshness rotation and the category leak are
all closed and verified live. Against that: Core Web Vitals were never near the
Discover threshold and last week's headline figure was not real; **Google is
de-indexing the site at scale**; and the sitemap carrying 92% of its URLs has
never been read by Google. The last of those was found only because the owner
pushed back on a number in this report.

**Production is healthy.** 10 of 10 probed surfaces answer 200; the newest
`/news-sitemap.xml` entry was **6.5 minutes old** at 12:07 UTC, which is the
quarter-hourly cron beat working. `node scripts/seo-health.mjs` against
production: **22 checks, ALL CHECKS PASSED, exit 0.**

**Search visibility is still falling, and one number in this report was
initially read the wrong way round.** The 28-day rollup looks good — **32
clicks, 1,940 impressions, 1.6% CTR**, against a 12 / 1,080 / 1.1% baseline on
08-21 — but 84% of those impressions come from Aug 15–21, before the outages.
Week over week the real figure is **1,641 → 282 impressions, −83%**. The
rollup is backward-looking; the trend is the thing that describes today, and it
is down. The decline is fully attributed to the logged incidents, which makes
it explained, not finished.

**And the indexing situation is worse than any earlier draft of this report
said.** Current data (GSC `Last update: 8/28`): **951 pages indexed, 1,720 not
indexed**, with **"Crawled – currently not indexed" at 562 against 10 a few
days earlier** — Google fetching pages and declining them, at scale. Against
**13,593** URLs the site publishes, Google knows about 2,671 and indexes 951.
An earlier draft of this report quoted a stale snapshot (1,520 indexed) and
used it to argue the de-indexation concern down. That was wrong; the concern
was right. The correction is kept visible in the GSC section rather than
quietly edited out.

**The mechanism, found only after re-checking: `archive-sitemap.xml` — 13,593
URLs, 92% of everything this site publishes — has never been processed by
Google.** GSC lists two sitemaps, not three. The two it has carry **953** URLs;
Google indexes **951**. `PLAYBOOK.md` has asserted all three were submitted
since 08-24, and every run since verified the file serves 200 rather than that
Google reads it.

**One thing now needs the owner, and it is 30 seconds** — see Owner blockers.

---

## Alerting first

One `[auto-alert]` issue is open: **#2, surface coherence, failing since
2026-08-23.** Production was probed before anything else and is fine, so the
audit was not preempted.

Issue #2 turned out to be **mostly a false red, and this run fixed that** —
see "What shipped" below. It stays open, correctly, for one real defect it was
hiding.

Two workflows that should have run this morning have not:

| Workflow | Schedule (Mon) | Last run | Fired today? |
|---|---|---|---|
| GSC template report | `40 7 * * 1` | 2026-08-27 (manual) | **No — 4h52m late at 12:32 UTC** |
| Core Web Vitals check | `17 7 * * 1` | 2026-08-24 (scheduled) | **No** |

Both fired within ~45 minutes on the previous Monday (Aug 24, 08:22 and
08:03 UTC), so this is not a broken schedule definition. I could not dispatch
them manually because the local `gh` token is invalid (the one owner item below).
**Consequence for this report: the GSC striking-distance list and indexation
buckets below are 4 days old**, and every GSC figure is labelled as such.

---

## Score: 86 → 83/100

Same weighting as previous weekly reports. Every component is justified by a
command or fetch run in this session.

| Facet | Weight | 08-21 | 08-24 | **08-31** | What the score is based on |
|---|---|---|---|---|---|
| Crawl infrastructure | 20 | 11 | 17 | **15** | `robots.txt` 200 advertising 3 sitemaps with `User-Agent: *` / `Allow: /`; `sitemap.xml` **348** URLs (340 on 08-24); `news-sitemap.xml` **639** entries, newest **6.5 min** old, oldest publication date **47.6 h** against the 49 h grace; `archive-sitemap.xml` **12,524** permanent URLs (5,891 on 08-24) with its growth finally reconciled (below); IndexNow key file 200 serving its own key; `www` 308 → apex; 11 category feeds + `/rss` valid; ISR live (`X-Nextjs-Prerender: 1`, `X-Vercel-Cache: HIT` on every surface); `seo-health.mjs` **22/22 PASS**. **+1 because the two crawl-breaking mechanisms are closed and proven, not just patched:** the archive write burst now beats twice an hour (the 10–14 h silent stalls that were losing stories to the void), and the retired-slug re-render closes the infinite 307 loop. **−2, found only after the owner pushed back on the indexing numbers:** GSC's Sitemaps report lists **two** sitemaps, not three — `archive-sitemap.xml` (**13,593** URLs, 92% of everything this site publishes) is advertised in `robots.txt` and has **never been processed by Google**. The two it does read carry **953** URLs; Google indexes **951**. Both were **last read Aug 24, seven days ago**. `PLAYBOOK.md` has claimed all three were submitted since 08-24 and every run since checked that the file serves 200 rather than that Google reads it — which is the same class of error as scoring a sitemap by its HTTP status. **Also held back by:** the 205 permanently lost story URLs, and the news sitemap still advertising retired slugs (`seo-health` logged **2 of 639** rename races; 1 of a 40-URL sample answered 307). |
| Story template | 20 | 16 | 17 | **18** | 40 story pages sampled at even intervals through `/news-sitemap.xml`; 39 answered 200 (the 40th was a rename 307). On those 39: **39/39** self-referential canonical, `index, follow` + `max-image-preview:large`, all JSON-LD parsing, `NewsArticle` + `BreadcrumbList`, `isBasedOn` present, `publisher.logo`, `datePublished ≤ dateModified`. Main content **median 408 words**. Outbound `/story/` links **median 4, zero on none**. **+1 for the freshness rotation (`a0d26f0`), which is a signal fix and not cosmetic:** before it, 150 of 726 live story pages had any re-render path, so 79% could serve a frozen `dateModified` and an under-reporting `isBasedOn` indefinitely; all 727 now re-render within ~6.5 h. **Held back by:** titles over 60 chars **34 of 39 (87%)**; descriptions ending mid-sentence **14 of 39 (36%)**; `/topic/` links **median 0, absent on 24 of 39 (62%)**. |
| List templates | 15 | 14 | 15 | **14** | All 26 list, section and report pages fetched: **26/26** answer 200, carry exactly one `h1`, and every JSON-LD block parses. `/space` 2,074 words, `/ai` 2,037, `/most-covered` 1,996, `/top-100` 1,558. `/general` and `/search` correctly `noindex, nofollow`. The ambiguity-guard fix (`015e2b9`) removed 40 wrong category placements from these pages' related rails — a real quality gain measured on production. **−1, for two defects this run measured that last week's 15/15 did not:** (a) the 08-28 Bing "descriptions too short" fix **overshot** on several pages — `/briefing` **273 chars**, `/reports/media-coverage` 235, `/most-covered` 178, `/energy` 173, `/obituaries` 166, all past the ~155–160 Google renders; (b) topic hubs collapse with the news cycle — of 6 sampled, `/topic/donald-trump` is **79 words / 2 links / noindex**, `/topic/ukraine` **30 words / 0 links / noindex**, `/topic/elon-musk` **31 / 0 / noindex**. |
| E-E-A-T | 15 | 14 | 14 | **14** | 11 typed, parsing JSON-LD pages hold. `/methodology/publisher-tiers` 792 words, `/methodology` 637, `/editorial-standards` 444, `/contact` 422, `/about` 316, `/news-desk` 312, `/corrections` 213, `/copyright` 129. Gained this week: the source-hub coverage profiles (`7457912`) are live and substantial — `/source/npr` **1,120 words / 45 links**, `/source/bbc-news` 772 / 28, both `CollectionPage` + `BreadcrumbList`. Lost this week, newly found: **all 12 trust pages are fully static with no revalidate** (confirmed in the build route table) and serve a **build-frozen masthead date** — measured `Age: 222,545 s` (2.6 days), rendering "Friday, August 28, 2026" on 2026-08-31. Net 0. Structurally still capped by the deliberately-deferred operator identity line. |
| AI search (GEO) | 10 | 8 | 9 | **9** | `llms.txt` 200, and **all 48 URLs it claims were fetched and answered 200**. It is also now *correct*: it claimed the lost stories answer "a 5xx that will not resolve", which has been false since they were tombstoned on 08-25 — probed 4 of them live today, **all 404** — and it contradicted its own preceding bullet. Fixed this run, along with adding the reuse terms. `robots.txt` allows every AI crawler. **First real evidence the channel works** (below): Bing reports **69 AI citations** in Aug 24–29, against 0 before Aug 21. Held back by: the 205 non-citable URLs, a single grounding query, and no per-entity structured answers. |
| Performance | 10 | 8 | 9 | **7** | Warm TTFB **98–203 ms** across 7 surfaces, every one a cache `HIT`; brotli wire weight **12–32 KB**. CLS **0**. **Authoritative numbers, from a real PageSpeed Insights run on CI** (the push-path trigger fired `cwv.yml`, which has the API key my local keyless probe does not): `/` **6,052 ms**, `/top-100` **3,602 ms**, a story page **4,965 ms**. Against PSI on 08-24 — 5,951 / 3,226 / 4,689 — that is **flat**. **−2 is a correction, not a regression:** the 08-24 report's headline "`/` LCP 1,852 ms, under the 2.5 s Discover threshold" came from an ad-hoc local run, while that same morning's PSI entry in `data/cwv-history.json` said **5,951 ms**. `/` has been ~6 s all along, against the 2.5 s Discover needs. My own local probe read 10–14 s and is ~2× inflated on this machine (see below) — its numbers are not quoted as absolutes. The eager-image fix shipped this run was verified live and did not move LCP. |
| Data quality & monitoring | 10 | 5 | 5 | **6** | `seo-health` 22/22. `url-survival` is **green in CI** (2026-08-30 run `success`) — the tombstone classification is proven, not just locally probed. Uptime probe green through 07:22 UTC today. Live `generalCategoryPct` **20.1%** (`/api/stats/coverage`) against the designed 34.8% — not a regression, as the 08-26 note requires checking. Publisher image weight recovered: **15 images, 1,525 KB, median 91 KB, max 189 KB**, so the 830 KB outlier logged on 08-29 was transient. **0 duplicate slugs.** The coherence gate is unblocked and now names a real defect instead of 16 non-defects (below). **+2: three monitoring surfaces that had never been read were read this run** — GSC **Manual actions** and **Security issues** (both clean) and the full **Crawl stats** report, which is the free log-file proxy the checklist has wanted since 08-24. **Held back, and revised down after the fact:** this report quoted the Page indexing report without reading its `Last update` stamp, published a stale figure (1,520 indexed) as current, and used it to argue down a real concern — the owner caught it, not any check here. **Nothing in this repo watches the indexed-page count or whether Google is still reading the sitemaps**, which is how a 13,593-URL sitemap went unprocessed and a 562-page de-indexation went unnoticed. Both are now backlog items 0a/0b. |

**Facet movement:** crawl infrastructure **−2**, story template **+1**, list
templates **−1**, E-E-A-T **0**, GEO **0**, performance **−2**, data quality
**+1**. Net **−3**.

Two of those are corrections rather than regressions — the site did not get
slower or lose a sitemap this week; both problems were already true and were
being scored as if they were not.

---

## What shipped this run

All gates passed before the push: **946 unit tests**, `tsc --noEmit` clean,
`eslint` clean, `npm run build` exit 0 with an unchanged route table, and the
Playwright suite.

### 1. The coherence alert was failing on the site's own cost controls

`[auto-alert]` #2 has been red every night since 2026-08-23. Today's first run
reported **16 violations** — and every one of them was an `archive-vs-live`
disagreement whose own cause label said *staleness* or *skew*, not *resolution*:

```
violations: 16  ->  list-side staleness 14, story-side staleness 1, skew 1
                    resolution: 0
```

Those are not defects. `/[category]` carries `revalidate = 3600`, and the cron
deliberately does **not** revalidate the dynamic category pattern — that nuke is
what blew the Hobby tier on 2026-08-24, and the playbook protects its absence as
a hard constraint. Measured directly rather than assumed: `/business` answered
**`Age: 3020`**, inside its own 3,600 s window, while carrying two of the flagged
clusters. The probe was failing the build for the site working as designed, and
burying the one class that is a real bug.

`scripts/surface-coherence.mjs` now fails **only** on a same-generation
disagreement, and reports the rest as findings counted by cause. The guard
against this becoming a silent pass is a stamp-coverage check in the same idiom
the file already uses for card extraction: if the `cw-live-dataset-version`
regex ever rots, every disagreement would classify as "unknown", land in
findings, and the probe would pass forever while blind — so archive renders
carrying the stamp below `MIN_COVERAGE` now fail as `extraction-blind`.
Measured today: **16 of 16 carried it.**

**The payoff, on the re-run:** `violations=1 findings=4`, and the survivor is a
genuine defect that 16 non-defects had been hiding — a **category flap** on
cluster `c31fffb88ca32`, alternating `business` → `politics` → `business` →
`politics` across four dataset generations. Issue #2 therefore stays open, and
is now open for a real reason. That flap is filed as a backlog item; the
playbook's rule that classifier defects are engineered upstream with benchmark
coverage means it is not a same-run fix.

### 2. The homepage was loading three full-size off-screen images eagerly

Measured, then root-caused in the markup. On a 412×823 mobile viewport under
Lighthouse's mobile throttle, `/` marked **four** images eager. One is the hero,
correctly `fetchPriority="high"` and the only image allowlisted through the
optimizer. The other three are the "More top stories" rail — and they render at
**y = 1203 / 1319 / 1435**, more than a viewport below the fold, as **raw
publisher originals** (976×549, 1024×683) because only the hero is optimized:

```
LCP element  /_next/image?...&w=1080&q=75   loadTime 11,353 ms   renderTime 13,876 ms
requests 33  images 6  declared image bytes 592,537
```

`EAGER_THUMBNAILS = 3` in `TopStoriesSection.tsx` is correct for the `lg:` grid,
where the rail sits *beside* the hero and is genuinely above the fold. At
`grid-cols-1` it stacks below it. Google scores the mobile viewport.

The fix keeps the reason the eager rail was added — the comment records that an
imageless hero would otherwise leave the page with no protected LCP candidate —
and pays for it only in that case: `eagerThumbnails = hero.imageUrl ? 0 : 3`.
Guarded by a new Playwright test asserting that when the hero has an image,
exactly one image is `fetchpriority="high"` and exactly one is eager.

**Verified live after deploy — and it did NOT fix LCP. Read this before
building on it.** The change works and does what it says: the homepage now
marks **1 image eager, 1 high-priority** (was 4 and 1), and the critical path
dropped from **6 image requests / 592,537 bytes to 5 / 437,713**. But three
post-deploy runs measured `/` at LCP **14,060 / 13,208 / 12,664 ms** — no
better than the 11,472 / 10,164 before it, and inside the same wide variance
band. So the images were a real inefficiency and **not the bottleneck**, and
this report's original framing of them as "the LCP budget" was wrong.

**What the bottleneck actually is, measured on the same runs:**

```
responseStart        120 ms    server is fast
responseEnd          410 ms    all 338 KB of HTML delivered
domInteractive     9,684 ms    <- 9.3 s of main-thread work, bytes already in hand
domContentLoaded   9,684 ms
loadEvent         14,649 ms
decodedBodySize  343,029 bytes
```

FCP came in at **9,332–11,944 ms** and LCP tracks it almost exactly, so nothing
paints for nine to twelve seconds. The network and the server are done at
410 ms. This is **main-thread cost — parsing 343 KB of HTML and hydrating a
large React tree under a 4× CPU throttle** — not image loading, not TTFB, and
not something a `loading` attribute can reach. The largest single script is
Google Analytics' `gtag.js` at **173,356 bytes**, which is the only script with
a reported content-length; the Next chunks report zero and were not
individually sized this run.

That is as far as the evidence goes. The fix is kept because fewer off-screen
full-size downloads is right regardless, and it is now guarded — but the
performance work that matters is payload and hydration cost, and it is next
week's item 2, re-aimed.

### 3. `llms.txt` was wrong about the site's own failure contract

Refreshed as the weekly task requires, and it needed more than a refresh. It
promised that the lost stories "answer a 5xx that will not resolve on a later
attempt". They have answered a plain 404 since the 08-25 tombstoning — probed
today, **4 of 4 return 404** — and the claim directly contradicted the bullet
above it ("A published story URL never answers 404"). Both are now correct and
consistent, the count is corrected from 214 URLs to **205 stories**, and the
reuse terms (`/copyright`, `/terms`, `/privacy`) are listed, which is the one
thing an AI crawler should be able to find and could not. All 48 URLs were
verified 200 before the edit.

---

## Core Web Vitals pass

**The KEYLESS PageSpeed Insights API is still unusable, confirmed again:**

```
HTTP 429 — "Quota exceeded for quota metric 'Queries' and limit
            'Queries per day' of service 'pagespeedonline.googleapis.com'"
```

The keyless per-day quota is zero, which is why the local probe falls back to
Playwright. **But CI is not keyless** — `PSI_API_KEY` is a repository secret,
so `cwv.yml` gets real PSI data, and firing it through the new push trigger is
what produced the authoritative numbers below. *A local run and a CI run of the
same script are measuring with different instruments; do not compare them.*
There is still no CrUX field data — that needs traffic volume this site does
not have yet.

**What curl proves — this is the good half:**

| Surface | TTFB | Brotli wire | Raw HTML | Cache |
|---|---|---|---|---|
| `/` | 130 ms | 32.4 KB | 338 KB | HIT |
| `/top-100` | 165 ms | 22.8 KB | 235 KB | HIT |
| `/politics` | 99 ms | 17.3 KB | 129 KB | HIT |
| `/business` | 134 ms | 17.0 KB | 129 KB | HIT |
| `/most-covered` | 166 ms | 23.7 KB | 229 KB | HIT |
| `/briefing` | 203 ms | 12.1 KB | 77 KB | HIT |

**CORRECTED after this report was first written — real PSI data arrived and my
local numbers were ~2× inflated.** The push-path trigger fired `cwv.yml` on a
clean CI runner, which *does* have `PSI_API_KEY`, so it produced genuine
PageSpeed Insights measurements. Those are authoritative and they disagree with
my local probe:

| Page | PSI 2026-08-24 | **PSI 2026-08-31** | my local probe, same day |
|---|---|---|---|
| `/` | 5,951 ms | **6,052 ms** | 11,472 / 10,164 ms |
| `/top-100` | 3,226 ms | **3,602 ms** | 4,948 ms |
| a story page | 4,689 ms | **4,965 ms** | 6,600 ms |

CLS **0** on all three.

**What I got wrong and why.** I ran a control — `example.com` at 640 ms,
`bbc.com/news` timing out at 90 s — and concluded the harness was sound. It
was: it can rank a fast page against a slow one correctly. But that control
only established *relative* validity, and I read it as licence to quote
*absolute* numbers. A 4× CPU throttle applied on top of an already-loaded
Windows workstation is not the 4× throttle Lighthouse applies on an idle CI
runner, and the gap is about a factor of two. The local probe is a
regression-detector, not a scoreboard.

**What survives unchanged, and it is the part that matters:** `/` at
**6.05 s** is still miles above the **2.5 s** LCP threshold that Discover
eligibility depends on, and the playbook ranks Discover as the third-biggest
lever on this site. TTFB (98–203 ms) and CLS (0) remain excellent. The
diagnosis is intact; only its magnitude was overstated.

**Week over week on the authoritative instrument: 5,951 → 6,052 ms. Flat.**
That is also the honest baseline the 08-24 report should have used — its own
PSI run that morning said 5,951 while the report quoted 1,852 ms from an
ad-hoc local run. So the −2 dock on this facet stands, and stands for exactly
the reason given: last week's headline number was never real.

The `domInteractive 9,684 ms` figure below comes from the same over-throttled
local probe, so it is inflated by roughly the same factor. **The shape of that
finding still holds** — LCP tracks FCP almost exactly, the network is finished
at `responseEnd`, and the cost is main-thread parse and hydration rather than
images — but a future run must re-derive the number from PSI or a clean
machine before sizing any work against it.

Fix 2 above removed three full-size off-screen downloads from the critical
path, and it was measured after deploy rather than assumed: **image requests
6 → 5, image bytes 592,537 → 437,713, eager images 4 → 1** — and **LCP did not
move** (14,060 / 13,208 / 12,664 ms over three runs). The images were not the
bottleneck. `domInteractive` at **9,684 ms** against `responseEnd` at
**410 ms** says the cost is main-thread parse and hydration of a 343 KB
document, which is where next week's performance work is aimed. The full
breakdown is in fix 2 above.

The remaining structural cost is that **only the homepage hero goes through the
image optimizer** — `next.config.ts` budgets the 5,000 transformations/month for
exactly one image, so `/politics` takes its LCP from a raw 1024×683 publisher
original for a 390 px slot. That is a $0-rule consequence, not a bug, and it is
not fixable without spend.

---

## Competitor content-gap snapshot

Free crawls and search only. Concrete gaps, with both sides measured.

**The gap is topic hubs, and it is the same shape as the source-hub gap that was
closed on 08-25 and worked.**

Ground News's topic pages carry roughly **20–25% durable, non-headline content**:
prose describing the topic, a cumulative article count over three months
(83,076 for AI), a "Covered Most By" publisher list, and related-topic links.
AllSides rates ~1,400 outlets as dedicated manual reference pages. Both rank on
evergreen, non-news demand.

CurrentWire's equivalents carry **nothing that outlives the news cycle**, and
they collapse when it moves on — measured live today:

| Topic hub | Words | Story links | Indexable? |
|---|---|---|---|
| `/topic/artificial-intelligence` | 2,012 | 33 | yes |
| `/topic/immigration` | 511 | 8 | yes |
| `/topic/climate-change` | 276 | 6 | yes |
| `/topic/donald-trump` | **79** | **2** | **noindex** |
| `/topic/ukraine` | **30** | **0** | **noindex** |
| `/topic/elon-musk` | **31** | **0** | **noindex** |

Three of six are 30–79 words and noindex themselves — and they are the highest
evergreen-demand entities on the list. A hub about Donald Trump that carries 79
words and disappears from the index whenever he has a quiet day is not a
reference page; it is a keyword filter over a 72-hour window.

**Why this is the actionable one, rather than a wish:** every input Ground News
uses, CurrentWire already computes — cumulative counts from the 12,524-URL
permanent archive, per-publisher breakdowns from `lib/news/source-profile.ts`,
and entity co-occurrence for related topics. The single input it lacks is a bias
rating, which it refuses to publish as a standing editorial position and which
this proposal does not need. This is the `hubStats` pattern that already shipped
once, on `/source/<slug>`, and measurably worked.

It also converges with three things already on the board: the 15 topic hubs that
GSC reports as **"URL is unknown to Google"**, the **~Sep 22 hub-consolidation
decision** (a hub with durable facts is defensible; a 30-word keyword filter is
the doorway-adjacent shape Google may simply be right about), and the AI-citation
finding below.

**Google News remains the unclosed channel and no crawl changes that:**
`data/gsc-queries.json` still reports `newsRows: 0` — zero query rows on the News
surface. The Google News homepage could not be crawled (JS-rendered), which is
itself the answer: there is no submission program to work and nothing to audit.

Sources consulted this run: [Ground News](https://ground.news/),
[Ground News AI topic hub](https://ground.news/interest/artificial-intelligence),
[StationX Ground News review](https://www.stationx.net/ground-news-review/),
[AllSides vs Ground News comparison](https://www.albis.news/perspectives/ground-news-vs-allsides-vs-albis-comparison).

---

## AI search — first hard numbers the site has ever had

Read from Bing Webmaster Tools → AI Performance (Microsoft Copilots and
Partners) in the owner's Chrome. GSC cannot show this.

| Date | Aug 21 | 22 | 23 | **24** | 25 | **26** | 27 | 28 | 29 |
|---|---|---|---|---|---|---|---|---|---|
| Total citations | 0 | 0 | 0 | **9** | 1 | **34** | 3 | 11 | 11 |
| Avg. cited pages | 0 | 0 | 0 | 1 | 1 | 2 | 1 | 2 | 2 |

**Total citations 69. Average cited pages 1.** Grounding queries: exactly one —
**"recent news roundup categories", 8 citations, 30.77% citation share.**
Bing's own caveat, from the page: the data shown is a sample of overall activity.

Two things follow. First, citations begin on **Aug 24** — the day the site came
back from the 402 and the retitle wave plus the expanded `llms.txt` deployed —
against zero for every prior day shown. Second, and more useful: the one query
that cites this site is a **category-roundup** query. The AI channel is asking
for exactly the surface Google has been de-indexing, which is an argument for
investing in hub and category depth rather than reading the Google verdict as
the only verdict.

Bing web search, same session, 7 days: **3 clicks, 46 impressions, 6.52% CTR**,
impressions starting Aug 27 (2 → 30 → 14). The queries are the site's actual
target shape — "news america and canada today", "us news today", "latest news
today from america and canada", "current situation in usa today" — all at
positions 8–10.

---

## GSC Crawl stats — read for the first time, and it explains the indexing problem

360°-checklist items 1–3, all three reachable after all: the property lives
under the `ovyajewels@gmail.com` profile, which is signed into the same Chrome
at `/u/1/`. Adding `/u/1/` to the URL was the whole fix.

| Check | Result |
|---|---|
| **Manual actions** | **No issues detected** |
| **Security issues** | **No issues detected** |

**Crawl stats (90 days) — the free log-file proxy, never captured before:**

```
Total crawl requests      80,800
Total download size       354 MB
Average response time     162 ms          (matches our own TTFB measurements)

By response      OK (200) 99%   404 <1%   302 <1%   5XX <1%   301 <1%
By purpose       Refresh 95%    Discovery 5%
By file type     Other 92%      HTML 5%   Image 1%   JS <1%   CSS <1%
By Googlebot     Desktop 92%    Other 4%  Smartphone 3%
Hosts            currentwire.us 80,817 ("Problems in the past")
                 www.currentwire.us 6 (no problems)
```

Three readings, and the first is the important one.

**1. Discovery is 5% of the crawl budget.** Google spends 95% of its requests
re-checking pages it already knows, on a site publishing ~700 new permanent
URLs a day. That is the measured mechanism behind the indexation sweep's
`not-crawled: 22` and behind all 15 topic hubs reporting "URL is unknown to
Google". The 08-25 backlog reasoned its way to "the actual constraint is CRAWL
DEMAND on a young domain" and shipped story→hub links on that theory; **this
is the first direct evidence for it**, and it says the theory was right.

**2. 92% of crawl requests are not HTML.** Only 5% are HTML pages. The site
serves a 2.4 MB `archive-sitemap.xml` that grows every day, three sitemaps, RSS
and 11 category feeds, plus a per-story Satori OG render. Whatever the exact
split, non-HTML fetching is consuming the budget that is not reaching the pages
that need indexing. This is worth a dedicated measurement next run — and it
gives the long-deferred `s-maxage` on sitemap/RSS responses (backlog item 0b,
"secondary levers") a real justification instead of a theoretical one.

**3. Desktop 92% against Smartphone 3%.** Google indexes mobile-first, so a
crawl this desktop-skewed is worth understanding rather than assuming benign.
Recorded, not diagnosed — one reading is not a trend.

**Crawl health itself is good:** 200s at 99%, 5xx under 1%, 162 ms average
response. The host's "Problems in the past" flag is the Aug 20 and Aug 24
outages, both closed.

---

## Search-engine coverage map — re-verified, holds

Every row of `seo/PLAYBOOK.md`'s table was re-checked by fetch today.

```
robots.txt                200, User-Agent: * / Allow: /, 3 Sitemap: lines
sitemap.xml               200, 348 URLs
news-sitemap.xml          200, 639 entries, newest 6.5 min old
archive-sitemap.xml       200, 12,524 permanent story URLs
IndexNow key file         200, serves its own key (d67fe7ac...)
llms.txt                  200, all 48 claimed URLs answer 200
www.currentwire.us        308 -> apex
```

Google, Bing, DuckDuckGo/Yahoo (via Bing), Yandex/Naver/Seznam (via IndexNow),
Brave (nothing possible), AI engines (robots + llms.txt): **all rows hold, no
change needed.** The map is additionally corroborated from the other side this
week — Bing is returning both web impressions and AI citations, which is the
first time the "via Bing" and "AI engines" rows have been confirmed by anything
other than our own configuration.

---

## Archive-sitemap growth — reconciled, and the alarm is cancelled

Carried as dated work since 08-26 and unreconciled for five days. Done now, two
independent ways, and they agree.

Per-day counts from the served `archive-sitemap.xml` `<lastmod>` values:

| Day | New URLs | |
|---|---|---|
| 2026-08-20 | **29** | the Neon egress outage |
| 2026-08-24 | 1,288 | |
| 2026-08-25 | 1,455 | |
| 2026-08-26 | **366** | the 14 h silent write stall |
| 2026-08-27 | **1,607** | stall fixed — catch-up burst |
| 2026-08-28 | 1,417 | catch-up continuing |
| 2026-08-29 | 711 | |
| 2026-08-30 | 681 | |
| 2026-08-31 | 408 (12 h) | ~800/day run rate |

Cross-check on totals: 11,122 URLs on 08-29 → **12,524** today is **+701/day**.

**The "doubling" was the recovery, not runaway growth.** Aug 27–28's 1,607 and
1,417 are the catch-up after the 10–14 h stalls; since the burst fix settled,
the rate is ~700/day — back inside the 450–700/day band the ledger showed
originally. The `seo-health` warn line is 45,000 URLs, so at this rate it is
**~46 days out (mid-October)**, not the ~30 days the backlog feared. Downgraded
from dated work to a watch item.

---

## GSC — read fresh from the dashboard, and it is better news than the API file

**Added after the report was first written.** The Monday workflow still has not
fired and `gh` still cannot dispatch it, so the *API* files are 4 days old — but
the dashboard itself is readable at `/u/1/`, and the two headline reports were
pulled from it directly this session. They change the picture materially.

**Search performance, last 28 days (through Aug 29), read today:**

| | 2026-08-21 baseline | **2026-08-31** |
|---|---|---|
| Clicks | 12 | **32** |
| Impressions | 1,080 | **1,940** |
| CTR | 1.1% | **1.6%** |
| Average position | 21.3 | **32.7** |
| Distinct queries | 217 | **298** |

**Do not read that table as good news — I did at first, and it is wrong.**
The full workflow data landed afterwards and the 28-day rollup is hiding a
continuing decline, because the window still contains the pre-outage peak:

| Week | Clicks | Impressions |
|---|---|---|
| Aug 15–21 | 26 | **1,641** |
| Aug 22–28 | 6 | **282** |

**Impressions −83%, clicks −77%, week over week.** Of the 1,943 impressions in
the 28-day total, **1,641 (84%) came from Aug 15–21** — the week that peaked at
438 in a day on Aug 16, before the outages. The daily series since is
19 (Aug 27), 6, 7, 2, 0, and while `lagDays: 3` means the last three are
undercounted, Aug 27 and 28 are not.

So the honest statement is: **the 28-day rollup improved against an older
28-day rollup, and the site's current run-rate is still falling.** A backward
window and a trend are different questions, and only the second one describes
today. Milestone M1 (100+ clicks/28d) reads 32, but on the current week's rate
it is moving away, not closer.

`data/gsc-daily.json` flags the decline `explained: true` — the incident ledger
accounts for it (the Neon egress outage, the 402, the archive write stall). It
being explained is not the same as it being over.

Three queries actually earned clicks this window — "summer mcintosh 400 free",
"the rise and fall of tony romo", "tupac autopsy photos" — against the top
impression pools that still earn none: **espn nba coverage cuts 69/0**,
**tommy john 26/0** (new, not in the 08-27 striking-distance list), jd vance
hometown book response 19/0, trump bbc lawsuit judge change 14/0, oswaldo
pirela ice detention 12/0, current events 9/0, moderna vaccine 8/0.

### CORRECTION — the indexing numbers below are STALE and I read them wrong

**Everything in this subsection was superseded within hours, and the error was
mine.** I read the Page indexing report without checking its `Last update`
stamp, quoted the snapshot as current, and used it to *soften* a concern the
backlog had been right about for a week. Re-read after the owner pushed back:

| | what I quoted | **actual (data as of 8/28)** |
|---|---|---|
| Indexed | 1,520 | **951** |
| Not indexed | 793 | **1,720** |
| Crawled – currently not indexed | 10 | **562** |
| Discovered – currently not indexed | 429 | **785** |
| Page with redirect | 69 | 90 |
| Excluded by noindex (by design) | 211 | 211 |
| Server error (5xx) | 2 | 2 |

**562 pages moved into "Crawled – currently not indexed" — Google fetched them
and decided not to index them.** That bucket was 10. This is active
de-indexation at scale, it is accelerating, and it lines up exactly with the
−83% week-over-week impression collapse. My "this materially softens the
de-indexation framing" was wrong in the worst available direction: I used a
stale number to argue down a live problem.

**It is NOT the site noindexing itself** — checked rather than assumed. The
thin-story policy (`lib/seo/story-indexing.ts`) noindexes single-source stories
past a 14-day window with zero GSC impressions, and the site turned 14 days old
during this window, so it was the obvious suspect. Probed the **33 oldest
archived story URLs** live: **33 of 33 serve `index, follow`.** "Excluded by
noindex" is flat at 211 — the deliberate ones (`/general`, `/search`, thin
hubs). Google is declining our pages on its own judgement.

### And here is the mechanism, which nobody had checked

The GSC **Sitemaps** report lists only **two** sitemaps:

```
news-sitemap.xml    Submitted Aug 14   Last read Aug 24   618 discovered
sitemap.xml         Submitted Aug 14   Last read Aug 24   335 discovered
                                                    total 953
GSC indexed pages                                         951
```

**953 submitted, 951 indexed — a 99.8% match.** Meanwhile:

```
archive-sitemap.xml   13,593 URLs   advertised in robots.txt   NOT IN GSC AT ALL
```

`seo/PLAYBOOK.md`'s coverage map has claimed since 2026-08-24 that all three
sitemaps are submitted. **Two are.** The third — which carries **92% of every
URL this site publishes** — is advertised in `robots.txt` and has never been
processed. Google knows about 2,671 URLs of 13,593.

That correlation is not proof (Google does know ~1,700 URLs beyond the
sitemaps, so it is not a hard 1:1), but it is the strongest single lever on the
board and it was invisible because every previous run verified the sitemap
*serves 200* rather than that Google *reads* it. Both submitted sitemaps were
also **last read Aug 24 — seven days ago.**

### The original (stale) reading, kept so the error is legible

```
Indexed                                          1,520
Not indexed                                        793
  Discovered - currently not indexed               429   <- the crawl-demand backlog
  Excluded by 'noindex' tag                        211   <- by design
  Not found (404)                                   69
  Page with redirect                                69   <- by design (merge 308s)
  Crawled - currently not indexed                   10
  Server error (5xx)                                 2
  Alternate page with proper canonical               2
  Duplicate, Google chose different canonical        1
```

~~**The site has 1,520 indexed pages.** The backlog's "Google de-indexed 11 of
12 indexed hub/category pages" is still true but was measured by a sweep over
34 evergreen surfaces only — a real problem on a small slice, not a site-wide
collapse.~~ **WITHDRAWN — see the correction above. It IS site-wide: 951
indexed against 13,593 published URLs, with 562 pages freshly rejected after
being crawled.**

**The 429 "Discovered – currently not indexed" is the same finding as the crawl
stats, from the other side:** Google knows about those URLs and has not spent
the budget to fetch them, and discovery is 5% of an 80,800-request crawl. Two
independent reports agreeing is why backlog item 5 is now measurement-led.

Also worth noting: **`Server error (5xx)` is 2** — the outage-era poison is
gone — and `Not found (404)` is 69 against 205 tombstoned stories, so Google has
processed only about a third of them so far.

---

### The API files (2026-08-27), for the fields the dashboard does not show

From the 2026-08-27 run. Per-query *positions* could not be pulled from the
dashboard — its metric toggles do not respond to synthetic clicks — so the
striking-distance list below is still the 08-27 one.

**Indexation sweep, 34 evergreen surfaces:** `indexed: 1`, `crawled-not-indexed:
11`, `not-crawled: 22`. This confirms the de-indexation recorded in the backlog —
only `/` remains indexed of the 12 that were. The 11 crawled-not-indexed pages
all carry `lastCrawlTime` of **Aug 15–16**, so Google has not re-fetched them;
this is index-selection, not a crawl failure, and the standing verdict applies:
no sitemap resubmission, no link-building, the levers are uptime and page value.

**Striking distance (position 5–20), 26 queries, still the CTR priority:**

| Page | Query | Impr | Clicks | Pos |
|---|---|---|---|---|
| `...c5a295c6ce624` | espn nba coverage cuts | 69 | 0 | 9 |
| `...cb3392289511d` | jd vance hometown book response | 19 | 0 | 7.2 |
| `...cf03400ab04ec` | trump bbc lawsuit judge change | 14 | 0 | 12.4 |
| `...c1ba75428433b` | rise and fall of tony romo | 5 | 0 | 9.6 |
| `/topic/uss-abraham-lincoln` | what is happening with the uss abraham lincoln | 2 | 0 | 19 |

`newsRows: 0` — Google News still shows this site essentially nothing.

The daily series is not judged this run: the last complete day in the file is
Aug 27 at 4 impressions, which is lag-undercounted, and the Aug 25–27
finalisation the backlog wanted to judge stabilisation on has not been pulled.

---

## Owner blockers

### 0. Submit `archive-sitemap.xml` to Search Console — 30 seconds, and it is the biggest lever on the board

Go to **[Search Console → Sitemaps](https://search.google.com/u/1/search-console/sitemaps?resource_id=sc-domain%3Acurrentwire.us)**
(the `/u/1/` account), paste this into "Add a new sitemap", press SUBMIT:

```
archive-sitemap.xml
```

**Why it matters:** GSC currently lists two sitemaps carrying **953** URLs
between them, and Google indexes **951** pages. The third sitemap — **13,593
URLs, 92% of everything the site publishes** — is advertised in `robots.txt`
and has never been processed. `PLAYBOOK.md` has claimed for a week that all
three were submitted; every run since checked that the file serves 200, which
is a different question.

I could not do this myself: Search Console's controls do not respond to the
browser extension's clicks (the same wall that blocked `gh auth login` — four
device codes, three tabs, every variant). Reading the dashboards works; writing
to them does not.

**Set expectations honestly:** Google is already declining pages it *has*
crawled (562 of them), so this will not by itself reverse the de-indexation.
It removes a hard ceiling on discovery; the value problem underneath is item 1.

---

### The rest — nothing affects the live site

The site is serving, the cron is beating, and every automated gate except the
two that did not fire is green. The GSC dashboards are readable — see below.

1. **The local `gh` CLI token is invalid** — `gh auth status` reports "The token
   in default is invalid". I worked around it by reading issues and workflow
   runs through the public REST API, which only works because the repository is
   public, and which cannot dispatch a workflow. *Fix: run `gh auth login` and
   pick "Login with a web browser".* Takes about a minute; nothing breaks until
   then.

   **Attempted and failed this session, so a future run does not repeat it:**
   re-authenticating through the Chrome extension does not work. The device
   flow gets all the way to GitHub's "Authorize GitHub CLI" screen with the
   one-time code accepted and the right account — and the final Authorize
   button never submits. GitHub's page raises its own *"Uh oh! There was an
   error while loading"* banner, and the same failure shape appears on
   Search Console's metric toggles: **these Google/GitHub SPAs do not act on
   the extension's synthetic clicks.** Three fresh device codes, two tabs,
   click/Enter/Space variants — all reached the same wall. Plain navigation
   and text entry work fine, which is why the GSC dashboards below were
   readable. Do not spend another run on it; it needs one human click, or the
   terminal command above.

   **The underlying problem is fixed a better way, so this is no longer
   blocking.** `gh` was only ever the means to one end — firing the two
   workflows — and that end did not actually need auth. `gsc.yml` and
   `cwv.yml` now carry a **push-path trigger**: change `.github/triggers/gsc`
   (or `cwv`) and push, and the workflow runs. Verified the minute it shipped:
   both fired with `event: push`. `.github` is excluded from `vercel.json`'s
   `ignoreCommand`, so the trigger files never cause a deploy. This also
   covers the half of the problem that was never about auth — the Monday
   schedules simply not firing. Running `gh auth login` is still worth a
   minute (it restores authenticated issue reads and `gh run` inspection) but
   nothing waits on it now.

   **And the first firing immediately earned its keep by breaking something.**
   Both workflows ran and both failed — with every data step green and only
   the commit-back red. Each ended in a bare `git push`, so a concurrent write
   to `main` rejected it and the run **discarded work that had already cost
   real quota**: a Search Console API pull and a full Playwright measurement.
   That was latent for as long as the two ran alone 23 minutes apart on
   Mondays; firing both in one push, with a docs push landing between them,
   made three writers on one branch. Both now rebase onto `origin/main` and
   retry five times with jittered backoff, and emit `::error::` naming the
   result as *generated but unsaved* if they truly cannot land. Re-fired
   afterwards against that same concurrent condition to prove the fix.

**Not a blocker, resolved during the run:** the GSC dashboards first answered
"you don't have access to this property" because Chrome's default profile is
`sameerhameedbaba@gmail.com` and the property belongs to `ovyajewels@gmail.com`.
Both accounts are signed into the same Chrome, and the second is `/u/1/` —
putting that in the path opened everything. Manual actions, Security issues and
Crawl stats are all in this report, the first time any run has had them. Future
runs: **use `search.google.com/u/1/search-console/...`**, do not report this as
blocked.

**Watch, no action:** this morning's scheduled GSC and CWV workflows had not
fired 4h52m past schedule (both fired within 45 min last Monday). If they run
later today the data lands by itself. If next Monday is the same it needs
looking at — and a working `gh` is what would let a run dispatch them rather
than wait.

The standing decision from 08-24 is unchanged and not yet due: **before 24 Sep**,
stay on Vercel Pro or downgrade. Do not downgrade before mid-September.

---

## Next week's top 5

**0. Judge whether the impression decline has bottomed — before anything else.**
Fresh data landed during this run (`30ed70f`, `dbacde6`): week-over-week
impressions **1,641 → 282, −83%**, with the daily series at 19 / 6 / 7 / 2 / 0
and `lagDays: 3`. Aug 29–31 will firm up within days. If they firm up flat or
lower, that outranks every content item on this list, because no amount of hub
depth matters on a site Google is showing less each week. If they firm up
higher, the outage fallout is unwinding on the 2–6 week schedule the 08-25
analysis predicted and the content work proceeds as ranked. **Re-fire the
report first — it is now one line:**
`date -u +"%Y-%m-%dT%H:%M:%SZ" > .github/triggers/gsc` and push.

The striking-distance list came back **unchanged** on fresh data — the same 26
entries, same top three (espn nba coverage cuts 69i/0c at pos 9, jd vance 19i
at 7.2, trump bbc 14i at 12.4). Stable targets across four days strengthens the
CTR case at item 3 rather than weakening it. `newsRows: 0` is also confirmed
fresh: Google News still shows this site nothing.

1. **Give topic hubs durable coverage profiles** — the source-hub pattern
   (`lib/news/source-profile.ts`, shipped 08-25, no new IO) applied to
   `/topic/<slug>`: cumulative story count from the permanent archive, the
   publishers that cover the topic most, first-seen, section mix, related
   topics. Highest-value item on the board: it is the measured competitor gap,
   it is what the ~Sep 22 consolidation decision needs, and it is the surface
   the AI channel is already citing. Needs no bias rating and no new spend.
2. **Attack the 9.3 seconds between `responseEnd` and `domInteractive`.** This
   week's image fix was verified live and did **not** move LCP, so the target
   is now correctly identified: 343 KB of decoded HTML and a large React tree
   costing nine-plus seconds of main thread before anything paints, while the
   server finishes in 410 ms. Two threads to measure before building
   anything — (a) how much of the homepage's 60-link payload has to be in the
   first response at all, and (b) whether `gtag.js` at 173 KB should move off
   `afterInteractive`, which is a trade against analytics fidelity on quick
   bounces and therefore an owner-visible decision, not a silent one. Do
   **not** repeat the eager-thumbnail change on `/top-100`, `/top-10`,
   `/most-covered` or `HubPage` on speed grounds until this is understood —
   the homepage result says it would not buy what it appears to.
3. **Rescue the CTR on the pages that already rank** — the striking-distance
   list, unchanged since 08-25 and still 509 impressions to 3 clicks. Use the
   sanctioned PREFIX-experiment infrastructure on the named targets. Blocked on
   fresh GSC data, so it moves when the workflow runs.
4. **Find out what the 92% non-HTML crawl budget is being spent on.** New, and
   it comes straight from the crawl-stats report above: only 5% of 80,800
   requests are HTML and only 5% are discovery, on a site adding ~700 URLs a
   day. If a 2.4 MB `archive-sitemap.xml` and the feeds are being re-fetched
   repeatedly, the `s-maxage` lever parked in backlog item 0b stops being
   theoretical. Measure before building: this is one reading, and the
   file-type breakdown does not say which files.
5. **Trim the overshooting meta descriptions** — `/briefing` at 273 characters
   and four others past ~160. Small, safe, and it undoes an overcorrection this
   loop shipped a week ago.

Just below the line, and ranked here so it is not lost: **root-cause the
category flap** on `c31fffb88ca32`, the real defect the coherence probe
surfaced once the 16 non-defects stopped hiding it. Classifier-deep, so it
needs benchmark coverage per the playbook, not a template patch.

Deliberately **not** ranked: adding `revalidate` to the 12 static trust pages to
fix the build-frozen masthead date. Hydration already corrects it for humans and
for Googlebot, the exposure is non-JS crawlers only, and buying that with new
ISR writes trades a real cost control for a cosmetic defect. Recorded in the
backlog with its measurement so a future run can decide with the number in hand.
