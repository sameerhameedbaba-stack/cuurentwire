# SEO daily run — 2026-09-02

Run 2026-09-01 21:40–22:35 UTC (2026-09-02 03:10–04:05 IST).

## Outcome first

**The website is fine. The pipeline that ships changes to it is not.**

Production has been serving correctly all day — fresh stories, valid sitemaps,
every health check green. But it has **not deployed any code since 2026-08-31
21:39 UTC**, over 24 hours, and nothing noticed, because every check this repo
owns tests the *site* and none tests the *build*. Three code commits are on
`main`, green in CI, and not serving — including yesterday's fix, which
yesterday's report marked "verified live".

**This is the one thing needing the owner: ~2 minutes on the Vercel dashboard**
to read why the builds fail. Push notification sent. Everything else in this
report is done or filed.

## 1. Alerting and production health — checked first

| Surface | Result |
|---|---|
| `https://currentwire.us/` | **200**, 0.55 s |
| `/archive-sitemap.xml` | **200**, 14,124 permanent story URLs |
| `/news-sitemap.xml` | **200**, 740 entries, newest 0.1 h old |
| `/rss` | **200**, valid with items |
| `/sitemap.xml` | **200**, 358 URLs |

`node scripts/seo-health.mjs` against production: **ALL 24 CHECKS PASSED**
(exit 0), including the 4-hour news-sitemap stall guard, 740 sitemap URLs
answering 200 with 3 benign mid-check rename races, 11 category feeds, llms.txt,
the IndexNow key file, story JSON-LD, the `www` 308 and a real 404.

Open `[auto-alert]` issues: **one**, #6 (url-survival). Triaged below — and it
turned out to be hiding two genuinely broken URLs.

## 2. Performance trend — checked before any other work

`data/gsc-daily.json` (generated 2026-08-31 15:21 UTC, `lagDays: 3`):

| Window | Clicks | Impressions |
|---|---|---|
| Aug 15–21 (prior 7) | 26 | 1,641 |
| Aug 22–28 (last 7 complete) | 6 | 282 |

**−76.9% clicks, −82.8% impressions, `explained: true`** — the window overlaps
the incident ledger (Neon egress outage Aug 20–21, the Vercel 402 on Aug 24,
the archive write-burst stall Aug 26–27, and Google de-indexing 11 of 12
evergreen hub/category surfaces). Per the standing rule this is named and not
escalated. The data is two days old; a refresh is dispatched with this run's
push (`.github/triggers/gsc`), within the once-a-day cap.

## 3. THE OUTAGE THAT ISN'T AN OUTAGE — production cannot ship code

Found while trying to verify this run's own fix: the post-deploy measurement
came back **exactly** equal to the pre-deploy one. Rather than blame CDN
caching, I looked for build identity, two independent ways:

**(a) An unconditionally-rendered string from a known commit.**
`components/layout/Footer.tsx:38` renders `<NewsletterSignup />` with no env
flag and no condition, added by `72e30e7` at 2026-08-31 21:39 UTC. Production's
footer renders every sibling (Corrections, Editorial Standards, Methodology,
News Desk, wordmark, tagline) and the form's own label `Daily briefing by
email` appears **zero times**.

**(b) Content-hashed asset filenames.** The homepage's
`/_next/static/immutable/chunks/*.js` names are **byte-identical before and
after** the push. A rebuild cannot leave content-hashed names unchanged.

**Code that never reached production** (everything else touched only `seo/`,
`data/` or `.github/`, correctly skipped by `vercel.json`'s ignoreCommand):

| commit | UTC | not live |
|---|---|---|
| `72e30e7` | 08-31 21:39 | newsletter signup form in the sitewide footer |
| `53ad4b1` | 08-31 22:27 | archive-row cache re-key — **yesterday's fix** |
| `6c63626` | 09-01 22:03 | flight-payload placeholder fix — today's |

**It is not the code.** `ci.yml` — typecheck, lint, unit tests **and
`npm run build`** — is `success` on all three, verified through the API.

**A correction this forces, and it is mine to own.** The 2026-09-01 report
marked `53ad4b1` SHIPPED and "verified live" because the frozen `308` was gone
afterwards. That report wrote the right caveat — *a deploy wipes the ISR cache,
so the 308 would have cleared on any deploy* — and the reality is worse than the
hedge: **there was no deploy**, and the symptom cleared for a third reason
nobody has identified. The mechanism is still proven by its unit tests; the
live claim was wrong. Today's fix is marked **MERGED, NOT LIVE** rather than
SHIPPED.

**Why every alarm stayed silent:**

1. `uptime.yml`, `seo-health.yml` and `url-survival.yml` all probe the site,
   and the site is fine — the cron and ISR keep feeding a stale build fresh
   data. Nothing asks *is production running the commit I pushed?*
2. The deploy signal is pinned. **Both** Vercel project integrations on this
   repo (`currentwire` and `cuurentwire`) report `failure` for **all 30** recent
   deployments, including ones that demonstrably shipped, so "failure" has
   carried no information for days.

**Owner action (~2 min, only you can):** Vercel dashboard → CurrentWire →
Deployments → open the newest failed build and read the log; and Settings → Git
to see whether two projects are still wired to this repo, disconnecting the
stale one. Filed as BACKLOG item **00** with the follow-up (`uptime.yml` should
assert a commit SHA, not just liveness).

## 4. Crawl samples — all clean

Three newest `/story/` pages from the news sitemap plus two section pages:

| Page | Canonical | Robots | JSON-LD |
|---|---|---|---|
| `…pa-police-officer-charged-…-c4b284fd010ee` | self ✓ | index, follow, max-image-preview:large | NewsArticle (pub 09-01 20:55Z, isBasedOn 1) + BreadcrumbList ✓ |
| `…palo-alto-networks-beats-…-c20fd28f38e82` | self ✓ | index, follow ✓ | NewsArticle (pub 09-01 20:30Z, mod 21:18Z, isBasedOn 1, Business) ✓ |
| `…stan-kroenke-agrees-to-buy-mlbs-angels-…-cad18106a4d23` | self ✓ | index, follow ✓ | NewsArticle (pub 09-01 20:00Z, mod 21:03Z, isBasedOn 1, Sports) ✓ |
| `/technology` | self ✓ | index, follow ✓ | BreadcrumbList + ItemList parse ✓, one h1, desc 162 chars |
| `/politics` | self ✓ | index, follow ✓ | BreadcrumbList + ItemList parse ✓, one h1, desc 163 chars |

No accidental noindex, no duplicated story, dates sane, sections correct.

## 5. Fixed: two story URLs had been serving 500s for twelve days

`[auto-alert]` #6 has been red since 2026-08-29 and yesterday's run triaged it
as "not an active regression". Re-reading it against `data/url-ledger.json`
showed that was half right. Today's numbers (probe of 2026-09-01 11:43 UTC,
7,252 URLs): `GONE=0 UNAVAILABLE=1 LOST=2 TOMBSTONED=212`.

The 212 tombstones are by design. The **1 UNAVAILABLE**
(`…-c09b64edf8f88`) is genuinely transient — the ledger shows
`lastOk: 2026-09-01T11:43:09.260Z`, i.e. it answered 200 during that very run;
a `TypeError: fetch failed`, not a 500. It must **not** be tombstoned.

The **2 LOST** were real damage nobody had acted on:

```
ccddfbfe6be27  firstSeen 2026-08-20T21:53:36.200Z   lastOk: null
cf6482c8299dc  firstSeen 2026-08-20T22:18:06.000Z   lastOk: null
```

`lastOk: null` — they have **never once served a 200**, in twelve days, and
both were first seen inside the Neon egress outage window. That is precisely
the profile the 2026-08-25 tombstoning was built for, and they were missed for
a sharp reason: that extraction filtered on `lastOk < "2026-08-22"`, and in
JavaScript **`null < "2026-08-22"` is `false`** — so the URLs that never worked
at all, the most certainly-dead cases, were exactly the ones the filter skipped.

Both are now in `data/lost-stories.json` (205 → 207 ids), so they answer a clean
404 instead of a crawl-poisoning 500, and `url-survival.yml` should go green —
restoring a gate that had been permanently red and therefore meaningless.
Guarded by a new case in `tests/unit/archive-outage.test.ts` that also pins the
UNAVAILABLE id as *not* tombstoned, so a future sweep cannot 404 live content.

## 6. Merged (not live): the placeholder every image shipped and one image showed

The strategy queue's item 1 is LCP 6.0 s → under 2.5 s, and backlog item 3 said
the target is main-thread cost with no named cause. Splitting the homepage
document found one.

**62% of the homepage HTML (213,382 of 343,323 bytes) is the RSC flight
payload**, not markup — the serialized React tree Next embeds and parses on the
main thread during hydration. Its key census pointed straight at the defect:
112 `stroke`, 84 `x1`/`y1`/`x2`/`y2`, 29 `cx`/`cy`.

`RemoteImage` is a Client Component, and **every prop a Server Component hands
to a Client Component is serialized into that payload, rendered or not**.
`StoryImage` was passing `fallback={<CategoryPlaceholder …/>}` — a ~1,045-byte
SVG element tree per image, to display only if that publisher's CDN had killed
the asset. Measured live, escaped fingerprint (in the payload) vs unescaped
(in the markup):

```
/                                28 serialized,  1 shown
/top-100                         25 serialized,  0 shown
/most-covered                    25 serialized,  0 shown
/topic/artificial-intelligence   31 serialized, 17 shown
/politics /technology /us         4 serialized,  0 shown  (each)
                  120.4 KB of 1,833 KB across nine pages = 6.6% of bytes
```

`RemoteImage` now takes the resolved category **label** (~26 bytes). Passing the
`CategoryId` instead would have dragged `config/categories.ts` — 26 KB of
classifier keyword dictionaries — into the browser bundle and lost more than it
saved, so the label is resolved server-side deliberately.

**Guarded twice, and the second guard is the point.** The natural e2e payload
assertion passes for the wrong reason: with no news API key the dev server
Playwright drives serves fixture stories whose art is all *local* placeholder
SVG, so `RemoteImage` never renders in CI. `tests/unit/story-image-flight-payload.test.ts`
drives the remote branch directly and fails naming the offending prop — proved
by reintroducing the defect and watching it fail, then reverting.

**Honest scope:** ~25 KB off a 343 KB page does not move a 9.7 s
`domInteractive`. This is a contribution to the LCP work, not a solution, and
it is **not live** until deploys work.

## 7. Also shipped: a way to tell "site down" from "I'm blocked"

Mid-run, Vercel's bot mitigation began answering **403
`X-Vercel-Mitigated: challenge` on every surface** — `/`, `robots.txt`, all
three sitemaps, `/rss` — for a browser UA, a Googlebot UA and plain curl alike.
Cause: this run's own deploy-polling (a 40-iteration loop with no sleep, then
one every 15 s). **Not a production outage** — the uptime workflow, probing from
GitHub's IPs, passed every check at 22:11:48 UTC — but from one vantage point it
is indistinguishable from one, and the standing rule makes an outage the run's
only priority.

`uptime.yml` now takes the push-path trigger `gsc.yml` and `cwv.yml` already
had (`81f0270`), so any run can buy a second opinion in about a minute instead
of guessing or waiting out a schedule that fires irregularly. Mitigation cleared
on its own at 22:17 UTC.

## 8. Gates

Both commits passed all five before push:

```
npx vitest run      956 passed, 3 skipped (63 files)
npx tsc --noEmit    exit 0
npx eslint .        0 errors (2 pre-existing warnings, unrelated)
npm run build       success
npx playwright test 119 passed, 1 skipped
```

**One honest caveat: the Playwright suite is flaky under full-suite load** —
about one test per run, a different one each time (`/most-covered` ItemList, the
sitemap reference-page list, `/search` exclusion). Verified **pre-existing**: on
a stashed clean tree it failed 2 of 3 runs the same way. This run's own new
tests passed 5/5 in isolation (30 executions). Filed as a finding, not fixed.

Also: an earlier full run showed 4 `/admin/auth` failures that were an artefact
of a leftover `npm run start` on port 3000 being reused by
`reuseExistingServer` — not a regression. Killed and re-run clean.

## 9. What regressed

Nothing on production. What regressed is the record: yesterday's "verified
live" claim for `53ad4b1` was false, and this run's own claim would have been
too had it not checked build identity.

## 10. What's next

1. **Owner: read the Vercel build log** (item 00). Nothing else ships until then.
2. Once deploys work: re-measure `6c63626` live and flip it to SHIPPED; confirm
   `url-survival.yml` goes green on the two new tombstones.
3. Add build identity to `uptime.yml` (a commit SHA in a header or
   `/api/version`) — the check whose absence hid a 24-hour shipping outage.
4. Resume the strategy queue: LCP remains item 1, with the payload threads in
   backlog item 3 re-derived against the corrected numbers (270 links, not 60).

## Owner blockers

**One.** Vercel deploys have failed since 2026-08-31 21:39 UTC and the build log
is only readable from the dashboard: Vercel → CurrentWire → Deployments → newest
failed build. While there, Settings → Git — two projects (`currentwire`,
`cuurentwire`) appear wired to this repo and both report every deploy as failed;
disconnecting the stale one restores a usable deploy signal. ~2 minutes.
