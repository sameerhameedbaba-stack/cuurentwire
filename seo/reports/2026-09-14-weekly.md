# SEO weekly deep run — 2026-09-14

Run 2026-09-14 11:50–12:3x UTC. Score **81 → 7/100 as served.**

---

# THE SCOREBOARD

**The site is down and has been for about 48 hours. Every page, every
sitemap and robots.txt answer `503 DEPLOYMENT_PAUSED`. And separately, the DNS
record that proves you own the site in Google Search Console is gone.**

Both need you. Neither can be fixed from code. Details are under *Needed from
the owner* at the end.

| Channel | Last 7 days | Prior 7 | Source, read this session |
|---|---|---|---|
| **Total visitors (GA4 users)** | **29** | 234* | GA4 User acquisition, Sep 7–13 vs Aug 31–Sep 6, read in the owner's Chrome |
| GA4 sessions | **36** | 250* | GA4 Traffic acquisition, same windows |
| — Direct | 26 sessions | 238* | same |
| — AI Assistant | **6** | 5 | same. The only channel that grew |
| — Referral | 2 | 3 | same |
| — Organic Search | 1 | 4 | same |
| — Organic Social (Bluesky) | 1 | 0 | same |
| **Google Search clicks** | **2** | 4 | `data/gsc-daily.json`, Sep 2–8 vs Aug 26–Sep 1. **Pulled 2026-09-11, not refreshed.** See below |
| Google impressions | **6** | 86 | same (−93%) |
| Google avg position (28d, web) | 31.6 | 31.5 | `data/gsc-report.json`, same pull |
| Google indexed pages | **not measurable** | 974 (09-07) | Search Console refuses the owner account (see §2) |
| **Bing AI citations** | **not measurable** | 0 | Bing Webmaster is signed out in the owner's Chrome |
| **Newsletter subscribers** | **0** | 0 | Buttondown Subscribers page: empty state |
| **Bluesky posts** | **26** (Sep 7–12), then **0** | 30 | public API, `currentwire.bsky.social`. 0 followers, 0 likes, 1 repost since Sep 7 |

\* The prior week is inflated by one event. GA4's own anomaly note says Direct
sessions hit **197 on Sep 3** against an expected 7, almost all from India, on
Chrome, to `/`. That is one burst, not an audience. Take it out and the prior
week was about 37 users. So **this week is flat against a real baseline of
~35–40 visitors a week**, and it will read lower next week, because the site has
served nothing since Sep 12.

**The GSC numbers are 3 days stale, and I am saying so rather than refreshing
them blindly.** The Monday `gsc.yml` schedule (07:40 UTC) had not run by 11:58
UTC, the third missed Monday. Its script reads `/news-sitemap.xml` and
`/api/stats/coverage` from production, and both answer 503. A refresh right
now would test the keep-previous-file path during an outage, not measure Google.

## What moved

- **The site went dark.** Production answered its last green uptime probe at
  **10:44 UTC Sep 12**. GitHub shows Vercel **"Deployment was blocked"** from
  **11:49 UTC**. The first failing probe was 14:06. It is still down.
- **Google ownership broke.** No `google-site-verification` TXT record exists
  on `currentwire.us` at any resolver, including Hostinger's authoritative
  nameservers. Search Console signed in as `ovyajewels@gmail.com` (avatar
  checked) says *"you don't have access to this property"*.
- **AI Assistant sessions went 5 → 6.** Tiny, but it is the only channel that
  grew, and it grew while Google sent 1 visit.
- **GA4 is readable again.** Last week's re-auth wall is gone.

## What did not move

- Googlebot. Its last sitemap downloads were **231.5 h / 487.3 h / 484.7 h**
  old at 13:22 UTC Sep 13 (monitor issue #11). That was before the ownership
  problem was visible.
- Newsletter: 0 subscribers, eleven days after the form went sitewide.
- Bluesky: 58 posts in total, **0 followers**.

## The single most important thing next week

**Get the site back up, then re-add the DNS record.** In that order. Each is a
few minutes in a dashboard you own. Nothing on this board can produce a visitor
while every URL answers 503. A domain left on 503 for days is exactly the
signal that made Google back off in August (hypothesis, not proven, as it was
then). This time it is site-wide and includes `robots.txt`.

---

# 1. The outage, with its evidence

Measured from this machine at 11:5x UTC:

```
/ /robots.txt /sitemap.xml /news-sitemap.xml /archive-sitemap.xml
/llms.txt /rss /about /api/stats/coverage /d67fe…abeca89.txt (IndexNow key)
www.currentwire.us/
  → 503   X-Vercel-Error: DEPLOYMENT_PAUSED   (no Retry-After)
body: "The deployment is currently unavailable  DEPLOYMENT_PAUSED"
```

**Timeline, from GitHub's public API:**

| UTC | Evidence |
|---|---|
| Sep 11 22:39 | `53bf6ad` deployment **success**. The last one |
| Sep 12 10:44 | uptime run #260 **success**. The last green probe |
| Sep 12 11:49 | deployment for `165605c`: **"Deployment was blocked"** |
| Sep 12 14:06 | uptime #261 fails → **issue #16** opened |
| Sep 12 15:37 | deploy-watch fails → **issue #17** |
| Sep 13 12:17 | deployment for `177b119`: **"Deployment was blocked"**; seo-health → **issue #18** |
| Sep 14 06:16 | uptime #273, still failing; 13 consecutive failures |

The monitoring worked. Five separate `[auto-alert]` issues opened, and issue
creation emails the owner. What did not happen is the fix.

**Cause: expected, not confirmed.** `DEPLOYMENT_PAUSED` is not the 402
`DEPLOYMENT_DISABLED` of Aug 24. It is the state Vercel's Spend Management
produces when the on-demand budget is hit with **Pause Projects: On**. As of
Sep 3 that budget was **$10**, with $5.42 already spent and 21 days left in the
cycle. I tried to read the billing page. Vercel is **signed out** in the owner's
Chrome, and I will not sign in on your behalf. So the cause is the obvious
suspect, and I am not presenting it as a finding.

**Do not turn Pause Projects off to fix this.** It is the only limit on a
runaway bill. Raise the budget instead. The billing cycle resets on the 24th,
and that is also the Pro-vs-downgrade decision date in `PLAYBOOK.md`.

Recorded as an **ongoing** incident in `data/incidents.json`. Every GSC decline
from Sep 12 onward is now annotated with it.

---

# 2. Search Console ownership: the record is gone

```
$ nslookup -type=TXT currentwire.us aurora.dns-parking.com   (authoritative)
currentwire.us  text = "v=spf1 include:_spf.mail.hostinger.com ~all"
```

The same single record comes back from 8.8.8.8 and 1.1.1.1. `_dmarc` is still
there, and the A record still points at Vercel (216.198.79.1). The
`google-site-verification=jXmPQ…` TXT recorded at domain verification on
2026-08-14 is **not**.

Search Console, owner's Chrome, `/u/1/`, **"Signed in as ovyajewels@gmail.com"**
shown on the page:

- `sc-domain:currentwire.us` → *Oops, you don't have access to this property*
- `https://currentwire.us/` → same

This is **not** the known wrong-profile trap (see memory
`gsc-dashboards-need-authuser-1`). The page names the correct account.

**What is not known:** when the record was removed. The service account behind
`gsc-crawl-freshness.yml` still read `sitemaps.list` at **13:22 UTC Sep 13**,
so API access outlived the record for a while, or the record went recently.
Today's scheduled crawl-freshness run (it usually lands 12:00–13:30 UTC) will
show whether the API has lost access too. The next run should read issue #11's
newest comment.

**Why it matters beyond dashboards:** Search Console is how this loop sees
Google at all. That includes the crawl monitor, `gsc.yml`, URL Inspection, the
Sitemaps report, manual actions and security issues. It also underpins
Publisher Center (auto-verified via GSC) and Bing Webmaster (imported from GSC).
Re-verifying restores the existing data. Google keeps it.

Recorded as an ongoing `google` incident, **dated to the day it was found**,
with an explicit note not to attribute search movement to that date.

---

# 3. Score: 81 → 7/100, as served

Same weighting as every weekly. **Every facet is scored on what production
serves this session, and it serves one 503 page.** I have not carried forward
any number from 09-07. The code behind the site is almost certainly unchanged
in quality: gates are green, see §5. But rule 2 says verify the outcome, and
the outcome is a 503.

| Facet | Weight | 08-31 | 09-07 | **09-14** | Evidence |
|---|---|---|---|---|---|
| Crawl infrastructure | 20 | 15 | 13 | **1** | `robots.txt`, all three sitemaps and the IndexNow key file all 503 with no `Retry-After`. The Search Console ownership record is missing. +1 only because the apex resolves, `www` hits the same edge, and a 503 is at least the retriable class, not the 402 of Aug 24. |
| Story template | 20 | 18 | 18 | **0** | No story page is served. `/story/x` → 503. |
| List templates | 15 | 14 | 15 | **0** | `/`, `/about`, `/rss` → 503. |
| E-E-A-T | 15 | 14 | 14 | **0** | Trust pages unservable. Manual actions and security issues **unreadable** (no property access). |
| AI search (GEO) | 10 | 9 | 7 | **1** | `llms.txt` 503. Bing AI Performance unreadable (signed out). +1: GA4 records **6 AI Assistant sessions** this week, 60% of all engaged sessions. These are real citations being clicked, and they came from pages served before Sep 12. |
| Performance | 10 | 7 | 7 | **0** | **Not measured, deliberately.** PSI against a paused deployment measures Vercel's error page. `cwv.yml` was not triggered. The LCP item stays open at its 09-07 reading (mean 4,772 ms on `/`, n=3) and is **not** re-claimed. |
| Data quality & monitoring | 10 | 6 | 7 | **5** | **Detection worked:** 5 auto-alert issues opened within ~2–3 h of the pause. Crawl monitor #11 still running correctly. **−2:** nothing watches the Google verification TXT, so an outage of this loop's whole Google view was found by hand. The Monday `gsc.yml` schedule missed again. And the item that would have exposed the Sep 12 pause first, a Vercel spend reading (backlog 00e), is still not built, because it needs an owner token. |

**Net −74.** Movement, labelled:

- **Genuine change, not correction: −72.** The outage took crawl, story, list,
  E-E-A-T, GEO and performance to what a 503 earns. When production answers 200
  again, these facets should be re-measured, not restored by assumption. The
  last code-state reading was 81, and it holds only until someone fetches it.
- **Genuine change: −2 on monitoring.** The missing DNS record is a new failure
  with no watcher.
- **No corrections** of previous scores this week. 09-07's 81 is not disputed.
  It simply no longer describes what is served.

---

# 4. Live deadline: archive-sitemap runway

**Not measurable this session.** `/archive-sitemap.xml` answers 503. Last
measured count **19,785** (2026-09-11 22:3x UTC, `seo-health.mjs`) against
`ARCHIVE_SHARD_AT = 45,000`. That is a recorded reading, not re-measured.

**No runway in days is given, and none should be.** The brief says "~1,059/day
net, fail() around 2026-09-24". That was **refuted with evidence on 2026-09-08**:
the route's predicate has a rolling 336-hour term, so the count falls as well
as rises. It dropped 290 in 9.5 h on Sep 7. Differencing it gives no rate at any
window size (`seo/MEMORY/2026-09-08-a-count-can-go-down.md`). What is true: at
19,785 the count sits 25,215 below the threshold, and while the deployment is
paused the cron is not running, so it is not growing. **The 2026-09-24 date in
the run brief should be deleted from it.** The plan is unchanged: shard routes
additively, then flip to `<sitemapindex>` together with the seo-health rewrite
in one commit, and never via `generateSitemaps`.

---

# 5. What ran, and what was deliberately skipped

**Ran:**

| Check | Result |
|---|---|
| `npx vitest run` | **1,076 passed**, 3 skipped |
| `npx tsc --noEmit` | clean |
| `npx eslint app lib components config scripts tests` | **0 errors**, 1 pre-existing warning (`tests/unit/cron-news-refresh-route.test.ts:52`) |
| `npx next build` | success |
| `npx playwright test` | **119 passed**, 1 skipped, 0 failures (local dev server) |
| Production probe, 11 surfaces | all 503 `DEPLOYMENT_PAUSED` |
| GitHub workflow runs + open issues (public API) | 6 open auto-alerts: #11 #14 #15 #16 #17 #18 |
| DNS TXT at 3 resolvers incl. authoritative | verification record absent |
| GA4, Buttondown, Bluesky public API | read, numbers above |
| Search Console, Bing Webmaster, Vercel billing | **blocked**: no access / signed out / signed out |

**Skipped, with the reason:**

- **CWV via PSI.** A paused deployment has no LCP to measure.
- **Competitor content-gap snapshot.** Our side cannot be fetched. Measuring
  only Ground News and AllSides would give a comparison with one side missing.
  The 09-07 gap (Ground News `/interest/donald-trump` 4,905 words indexable vs
  our 745 noindex) is the latest evidence, but I have not re-measured it.
- **`llms.txt` refresh.** No pages changed this week (the only code commits
  since 09-07 are scripts and tests), and live verification is impossible.
- **Coverage map re-verification.** Done in the only sense possible: every row's
  endpoint is 503, and the Google row now also lacks ownership. `PLAYBOOK.md`
  has been updated to say so.
- **Striking distance.** `data/gsc-queries.json` (pulled 09-11) still has the
  same 26 entries and the same top three: `espn nba coverage cuts` 69 imp / pos
  9, `jd vance hometown book response` 19 / 7.2, `trump bbc lawsuit judge
  change` 14 / 12.4. It stays deprioritised for the 09-07 reason. These are
  August impressions, and the site earned **0** impressions Sep 8–11.
- **No code shipped.** Nothing in the repo causes either problem, and a code
  push cannot deploy while Vercel is blocking deployments. Shipping sharding or
  hub work into a blocked pipeline would create unverifiable changes. That is
  the "no claim without evidence" failure in advance.

## 5a. Gate note

All five gates were run in full before pushing, on a tree whose only changes
are `seo/` files and `data/incidents.json`. No application code changed.

---

# Next week's top 5

1. **Confirm the site is back:** 200 on `/`, `robots.txt`, all three sitemaps,
   `llms.txt`. Then close the Sep 12 incident with an `end` date and close
   issues #14–#18 as their workflows go green.
2. **Confirm Search Console ownership is restored:** the property opens under
   `/u/1/`, and `nslookup -type=TXT currentwire.us` shows the
   `google-site-verification` record. Then read manual actions, security
   issues, crawl stats and the Sitemaps report that this run could not.
3. **Build a DNS-record watcher.** A daily step asserting the
   `google-site-verification` TXT exists (a public DNS query, no secret, $0),
   folded into `gsc-crawl-freshness.yml`. This week it would have fired on the
   day the record went.
4. **Give `gsc.yml` a freshness assertion** (carried from 09-12): fail when
   `generatedAt` did not move. It was the next-run candidate before the outage.
   It is still the highest-value item completable from code.
5. **Re-measure everything this report could not:** three PSI samples on `/`,
   the 30-page story sample, the 31 list pages, the competitor gap, and the
   archive count. The score only returns to the 80s on those fetches, not
   before.

---

# Needed from the owner

**Two dashboard actions, both urgent, in this order:**

1. **Un-pause the site on Vercel.** `vercel.com` → team `itsme-523d` →
   Settings → Billing → **Spend Management**. Raise the on-demand budget and
   resume the project. **Leave "Pause Projects" on.** It is the only cap on a
   runaway bill. If the page shows a different cause (payment failure,
   something else), that is new information. Please tell me what it says.
2. **Re-add the Google verification record.** Search Console (as
   ovyajewels@gmail.com) → Add property → **Domain** → `currentwire.us` → copy
   the `google-site-verification=…` TXT value → Hostinger → DNS for
   `currentwire.us` → add a **TXT** record, name `@`, that value → back in
   Search Console, click **Verify**. The existing data comes back with it.
   Please also look for anything else that changed in that DNS zone at the same
   time.

**While you are signed in to Bing Webmaster**, the AI-citation numbers for this
scoreboard are at Bing Webmaster → AI Performance. I could not read them this
week.

**Standing, unchanged:** the Pro-vs-downgrade decision is due **Sep 24**. That is
also the day the billing cycle resets. A read-only Vercel API token (backlog
00e) would let a workflow warn before the budget pauses the site again, instead
of after.
