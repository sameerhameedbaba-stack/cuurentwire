# SEO daily run — 2026-09-15 (WATCHDOG, cloud)

Site still down; the reason is now **confirmed** and it is not what the
previous report guessed. No code changed, nothing deployed, nothing paid.

## CORRECTION — the 402 is a usage pause, not an unpaid invoice

The 2026-09-14 report's owner-checklist item 1 told the owner to open the
Vercel billing dashboard and look for an outstanding invoice. **That was
wrong**, and it contradicted a memo already in this repo
(`seo/MEMORY/2026-09-15-owner-working-style-and-system-map.md`, "Why the site
is down right now"), which had it right. The error was reading the 402's
generic body string "Payment required" as a statement about this account.

Owner-verified on the Vercel dashboard, 2026-09-15:

- **No unpaid invoice.** Every invoice reads **Paid** — August: Pro $23.60,
  Neon marketplace $11.35, Observability $0.
- The 402 is the **free Hobby-plan usage pause**. The team dashboard reads
  *"Paused – Upgrade to resume service"*, driven by the Pro-period usage
  sitting over the Hobby caps:

  | Meter | Used (Pro period) | Hobby cap |
  |---|---|---|
  | Fluid Active CPU | 122h 33m | 4h |
  | ISR Writes | 4.8M | 200K |
  | Fast Origin Transfer | 61.56 GB | 10 GB |
  | Edge Requests | 2.1M | 1M |
  | ISR Reads | 1.3M | 1M |
  | Function Invocations | 1.2M | 1M |

- **It lifts by itself at the billing-cycle reset, ~2026-09-24.** The only
  button Vercel offers is Upgrade, which the owner refuses under the $0 rule.
- The older "~$65.91 final Pro invoice / $14.74 Neon" figures were
  *projections of an in-flight cycle*, not amounts billed. They never posted
  as stated. Corrected at source in the cloud-migration memo.

**Standing consequence: until ~2026-09-24, a 402 `DEPLOYMENT_DISABLED` on
this project is EXPECTED.** It is one quiet line in the report — never a
billing alarm, never an owner checklist item, never an Upgrade suggestion.
Re-escalate only if (a) it is still 402 after the reset, (b) the status code
or `x-vercel-error` value changes again, or (c) same-run evidence
contradicts the usage-pause reading.

## Live status measured this run (2026-09-14 21:46 UTC)

| Check | Result | Evidence |
|---|---|---|
| `GET /` | **402** | `x-vercel-error: DEPLOYMENT_DISABLED`, body "Payment required", `x-vercel-id: iad1::5grhf-…` |
| `GET /archive-sitemap.xml` | **402** | same error class |
| google-site-verification TXT | ✅ **PRESENT** | `google-site-verification=jXmPQSZbqumoPn0AKH4swGuYMoUDBFkJ4oq_4PrD2rY`, TTL 14400 |
| Apex `A` | ✅ `216.198.79.1` (Vercel) | dns.google |
| `www` CNAME | ✅ `0f5a5c271e8e5440.vercel-dns-017.com.` | dns.google |

DNS read two ways — **dns.google and cloudflare-dns.com agree** on the TXT
set (SPF + the google-site-verification record). This is a real change from
every check since 2026-09-14, when the apex carried only SPF.

## Search Console ownership — RESOLVED

The owner re-added the TXT at Hostinger and re-verified the property. The
`data/incidents.json` entry is closed with `end: 2026-09-15`. Off the owner
checklist. Re-open only on a failing DNS query made in the same run.

## GSC numbers — not re-fetched, outage caveat stands

Newest committed data (`data/gsc-report.json`, generated 2026-09-14T14:22Z,
28-day window): 433 URLs / 1,296 impressions / 40 clicks. These predate
nothing that happened today and reflect the outage collapse already recorded.
Not evidence of anything current — do not carry them forward.

## Files changed this run (docs/data only, by owner instruction)

- `data/incidents.json` — 402 entry corrected to the confirmed usage-pause
  cause; Search Console ownership entry closed with an `end`.
- `seo/routines/daily.md` — watchdog now triggers on 503 **or 402**; adds the
  standing "402 is expected until ~2026-09-24, do not raise a billing alarm"
  rule with explicit re-escalation triggers.
- `seo/routines/weekly.md`, `seo/routines/offpage.md` — same trigger fix so a
  402 is not missed or misread.
- `seo/PLAYBOOK.md` — coverage-map note and the 2026-08-24 standing 402
  caveat amended: owner-blocking ≠ owner-actionable.
- `seo/BACKLOG.md` — top block and precedence header corrected (outage cause
  confirmed; Google ownership marked fixed).
- `seo/MEMORY/2026-09-15-cloud-migration-and-current-state.md` — invoice
  projections and the TXT/site-status paragraphs corrected.
- `seo/MEMORY/2026-09-15-a-generic-error-string-is-not-a-fact-about-you.md` —
  new lesson note.

## Owner checklist

**Nothing.** No dashboard step, no DNS step, no payment. The pause clears
itself ~2026-09-24.

The only open item is a decision, not a task: **the scope of the slim
relaunch**, which the owner has explicitly deferred. Build work is on hold
until they give the go-ahead. Relaunching the current aggregator unchanged
would re-exhaust the caps in days — that is the constraint the scope has to
respect.

## Re-check, same day (2026-09-15 21:36-21:37 UTC)

A second cloud firing re-measured everything above; nothing changed.

| Check | Result | Evidence |
|---|---|---|
| `GET /` | **402** | `x-vercel-error: DEPLOYMENT_DISABLED`, `x-vercel-id: iad1::mhsf5-1789508214634-866baee775cf` |
| `GET /archive-sitemap.xml` | **402** | same error class |
| google-site-verification TXT | ✅ present | `dns.google` and `cloudflare-dns.com` agree on `google-site-verification=jXmPQSZbqumoPn0AKH4swGuYMoUDBFkJ4oq_4PrD2rY`, TTL 14400 |
| Open `[auto-alert]` issues | 6 open (#11, #14, #15, #16, #17, #18) | all created 2026-09-07 to 2026-09-13, i.e. predate and describe the known outage/usage-pause — none new today |
| `data/gsc-daily.json` trend | decline `explained: true` | unchanged since 2026-09-14T14:22Z generation; not re-fetched (site still down, nothing new to fetch) |

Still inside the expected 402 window (lifts ~2026-09-24 per the Hobby
billing-cycle reset). No re-escalation trigger fired: status code and
`x-vercel-error` are unchanged, TXT still resolves, no same-run evidence
contradicts the usage-pause reading. No files other than this report
changed this run.
